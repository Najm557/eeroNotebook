# Implementation Plan: eeroNotebook v1

## Overview

This plan stands up eeroNotebook on the Dev Server in two distinct movements. Tasks 1–4 deploy an **unmodified upstream Open Notebook** against Host Ollama and prove the infrastructure works — runtime, database, inference, embeddings, citations. Only then do tasks 5–9 begin diverging from upstream, because debugging a fork and debugging a deployment at the same time is avoidable.

Tasks marked **(\*)** implement a decision made by recommendation rather than discussion; see *Deferred decisions* in [design.md](design.md).

Models are already installed: `qwen2.5:14b` and `nomic-embed-text` were pulled during planning.

## Tasks

- [x] 1. Establish the fork and workspace
  - [x] 1.1 Fork and clone
    - Forked to `Najm557/eeroNotebook` — the account has no organisations, so the fork sits on the personal account
    - Workspace root is the repository; `upstream` points at `lfnovo/open-notebook`, `origin` at the fork
    - Tag `v1.14.0` checked out onto branch `eero`, which is pushed and tracking
    - _Requirements: 14.1, 14.2_
  - [x] 1.2 Retire evaluation clones
    - Removed `research/`, reclaiming 561 MB
    - `CONTEXT.md`, `docs/adr/` and `.kiro/specs/` survive and are committed
    - `.gitignore` needed a scoped negation: upstream's broad `specs/` rule would otherwise exclude `.kiro/specs`
  - [x] 1.3 Record the deployment target
    - `deploy/.env` holds `EERONOTEBOOK_HOST=10.17.8.52`, generated on the host, gitignored **(\*)**
    - `deploy/.env.example` is the committed template
    - _Requirements: 13.7_

- [x] 2. Build the Inference Gateway
  - [x] 2.1 Establish how a container reaches the inference backend
    - Verified against the live host: a container reaches `http://host.docker.internal:11434` and receives `200`, with Ollama bound to `127.0.0.1`
    - Verified the host's LAN address does not work — `10.17.8.52:11434` answers nothing, from a container or from the host itself
    - Verified the model server cannot usefully be containerised here: `llama3.2:3b` ran at 87.8 tok/s native against 0.50 tok/s in a container, with `library=cpu` and no `/dev/dri`
    - Conclusion: Ollama stays on loopback as a backend; only the gateway crosses to it
    - _Requirements: 3.1_
  - [x] 2.2 Add the gateway container to the stack
    - `eeronotebook-inference` runs LiteLLM from `deploy/litellm-config.yaml`, no database, unpublished
    - Serves both routes; verified `/v1/embeddings` returns 768 dimensions and `/v1/chat/completions` answers
    - Reaches the backend via the host alias with `extra_hosts: host-gateway`, per `~/stacks/earovoice/`
    - Requires a credential: verified an unauthenticated request to `/v1/models` returns `401`
    - The image ships no `curl`, so the healthcheck probes with its own Python — a curl-based probe left the container permanently unhealthy and blocked the app on the dependency
    - _Requirements: 3.1, 3.6, 13.2, 13.9_
  - [x] 2.3 Map logical model names onto backend models
    - `eero-synthesis` → `qwen2.5:14b`, `eero-background` → `llama3.2:3b`, `eero-embed` → `nomic-embed-text`
    - Mapping lives only in gateway configuration
    - `qwen2.5:14b` confirmed tool-capable, which Requirement 8.2 depends on
    - No synthesis-to-background fallback configured: a silent downgrade would weaken grounded answers with no signal
    - _Requirements: 3.3, 3.7_
  - [x] 2.4 Verify the boundary holds
    - Verified the app reaches chat and embedding routes through the gateway alone, and that responses carry the logical name (`model=eero-synthesis`) rather than the backend model
    - Verified no host address or model server address appears in the app container's environment
    - Container healthcheck in place; the Uptime Kuma monitor is task 4.2
    - Verified with the gateway stopped: chat answers `502` and vector search `500`, both naming the fault, and both recover when it restarts
    - Source ingestion did degrade silently: embedding is a separate fire-and-forget job, so an unembedded Source read `completed` with no error while the real failure sat on a command record nothing pointed at
    - Fixed by reporting a failed `embed_source` job as a failed Source, which lands in upstream's existing failed-source UI and needs no frontend change; suppressed once the Source has embeddings, so a successful retry clears it
    - Configuration could not carry this — both embedding paths are fire-and-forget — so it is the first application code change, arriving before task 3.4's unmodified-upstream checkpoint
    - The report appears when the embed job exhausts its five retries, roughly a minute, rather than on first failure
    - **Closed out against the live deployment; everything above still holds.** The findings from here up are the earlier session's and were re-checked rather than restated. The fix is committed as `2fef1cf` and is still the most recent change to `api/routers/sources.py` and `open_notebook/domain/notebook.py`, so nothing since has overwritten it, and it is present in the running image. `tests/test_source_embedding_failure_visibility.py` pins it with 12 checks: the reporting rule itself, all three endpoints a member sees, extraction failure taking precedence, and a stale failure cleared once embedded
    - Requirement 3.8 re-verified on both sides of the boundary. The app service in `deploy/docker-compose.yml` names no host and no model server address, and neither does the running container's environment — no `host.docker.internal`, no `10.17.8.52`, no `:11434`, no `OLLAMA_*` — and its `ExtraHosts` is empty, so the application has no route to the host even if something were misconfigured. Its other source of addresses is the stored credential, which reads `http://eeronotebook-inference:4000/v1`; the three registered models are the logical names only, with no backend model name anywhere in the application
    - **Requirement 3.8 is now held by a test rather than by a hand check.** `tests/test_inference_boundary_config.py` reads the compose project and fails if the app service names a host or model server address, if it declares `extra_hosts`, or if any service other than the gateway addresses the backend — plus one check that the gateway *does*, so the other three cannot pass vacuously. Verified it bites: adding `OLLAMA_API_BASE: http://host.docker.internal:11434` to the app service fails two of them by name. The tempting fix for an inference problem is to point the application straight at the model server, and that is the edit this catches
    - Boundary re-verified end to end: the application's own model tests reach both routes through the gateway (chat answered, embeddings 768 dimensions), and gateway responses still carry the logical names — `model=eero-synthesis`, `model=eero-embed` — rather than `qwen2.5:14b` or `nomic-embed-text`. An unauthenticated `/v1/models` from the app container still answers `401`
    - **The embed-failure report re-verified against real rows, with the gateway left running.** A throwaway Source carrying a failed `embed_source` record of the shape the real fault produces read `failed` on all three surfaces — detail, `/status` and the list — each naming `eero-embed`; embedding it for real then cleared the report, so the suppression branch works against live data and not only against mocks. That exercised the SurrealQL the unit tests mock out, which is the half that could have silently matched nothing. Everything created was deleted: sources, embeddings and notebooks are back to 0 and the queue back to 57 completed / 16 failed. Two of those 16 are genuine `embed_source` failures carrying the gateway-unreachable message, from before the fix
    - **Not re-verified, deliberately:** the chat `502` and vector search `500` measured with the gateway stopped. Stopping it now would take the application down on a shared host and alarm the monitor, to reproduce a result already recorded. The mechanism is consistent with those two numbers and readable in the code — graph nodes classify an unreachable backend through `classify_error`, `api/main.py` answers `NetworkError` and `ExternalServiceError` as 502, and `api/routers/search.py` wraps its own failure as `500 Search failed: …` before a handler can map it, which is why search was the odd number. `tests/test_search_api.py` covers the vector path raising rather than returning an empty result, which is the silent degradation 3.5 forbids
    - Requirement 13.10 confirmed, not rebuilt: Kuma monitor 23, `eeroNotebook Inference Gateway`, probes `http://eeronotebook-inference:4000/health/liveliness` by keyword every 60 s and its latest heartbeat is up (`200 - OK, keyword is found`), with the stack group reporting all children up. The container healthcheck is healthy, and the gateway is still unpublished while sitting on `monitoring_net`, so task 4.2's added reachability still grants nothing
    - Noticed while checking: the queue holds no `new` and no `running` records even though `eeronotebook-db` had restarted five hours earlier — the condition that silenced the worker in task 3.4. One observation, not a test of the watchdog, but the failure it exists for was not present
    - Gates: `uv run pytest tests/` 727 passed / 18 skipped — 723 as of task 5.6 plus the four new boundary checks — with `uv run ruff check .` and `uv run python -m mypy .` clean across 145 source files
    - _Requirements: 3.5, 3.8, 13.10_

- [x] 3. Deploy the stack, unmodified
  - [x] 3.1 Write the compose project
    - `deploy/docker-compose.yml`, project `eeronotebook`, deployed at `~/stacks/eeronotebook/`
    - App built from this branch rather than pulled, so image and source cannot drift
    - 8502 and 5055 published; database and gateway unpublished
    - Named volumes `db_data` and `app_data`; `eeronotebook_internal` plus external `monitoring_net`
    - Dozzle labels applied, matching the convention in `~/stacks/llm-server/`
    - _Requirements: 13.1, 13.2, 13.3_
  - [x] 3.2 Configure environment
    - All four secrets generated on the host into `deploy/.env`, mode 600, never transiting a client
    - SurrealDB credentials replaced; compose fails fast if any secret is unset
    - `OPEN_NOTEBOOK_WORKER_MAX_TASKS=1` applied to the worker supervisord runs in-image
    - `OPEN_NOTEBOOK_PASSWORD` set as the interim gate until task 5
    - _Requirements: 13.4, 3.4_
  - [x] 3.3 Point the application at the gateway
    - One `openai_compatible` credential at `http://eeronotebook-inference:4000/v1`; connection test passed
    - Discovery returned the three logical names; all registered
    - Defaults set to `eero-synthesis` for chat, transformation, tools and large-context, and `eero-embed` for embedding
    - **Outstanding:** confirm chunk sizing respects the embedder's 2048-token window
    - _Requirements: 1.3, 1.4, 3.1, 3.8_
  - [x] 3.4 Validate the baseline end to end
    - `tests/integration/test_baseline.py` drives the deployed API, skipped unless `EERONOTEBOOK_API_URL` and `EERONOTEBOOK_API_TOKEN` are both set. With them unset, `uv run pytest tests/` is 649 passed and 17 skipped; the checks are the subset of `.codex/agents/smoke-e2e.toml` this task gates, so podcasts and transformations are deliberately absent
    - Run against `http://10.17.8.52:5055`, not `https://api.eeronotebook.local` — task 4.1's front door has no DNS entry and a certificate from a CA nothing trusts, so it is not reachable from a client yet. Password read from the host's `deploy/.env` into the run's environment only
    - **17 of 17 green, twice consecutively, ~4m15s per run.** Every criterion was exercised against the live stack: all three Source kinds accepted and extracted (uploaded document 112 tokens, Wikipedia page 13,500, TED transcript 2,982), each embedded with chunks present; the unresolvable `.invalid` URL failed alone and kept its reason while the three Sources queued around it completed; vector search returned scored results reaching this run's Sources (0.82 on the uploaded document, 0.52 on the web page); the answer carried citations that resolved to real Source ids over the API, and a retrieved passage was located inside the cited Source's own text; the uncovered question was refused with none of the Krebs-cycle intermediates present; deleting the Notebook cascaded and left no orphan Source
    - The first run failed two checks and **both were defects in the test, not the deployment**. The refusal check's phrase list did not contain "did not provide information", which is the wording the model actually used — widened. And the chat check passed `context_config: {}`, which is not a neutral default: `build_notebook_context` then takes the *short* context path, carrying each Source's id, title and insights and **no text at all**. Asked for a specific interval on that context the model answered "21 days" where the document says 23, and cited the document for it. Fixed by requesting `"full content"` per Source, after which the answer is correct
    - That accident is worth keeping: commit `c3b7967`'s grounding instructions live in `prompts/ask/*` only, so the **chat path carries no such instruction** and will invent a cited answer from an empty context. Requirement 2.5 is currently met on the ask path and unenforced on chat
    - Requirements 2.2, 2.3 and 2.5 held on all three runs (2.5 refused correctly even on the first; only the assertion's vocabulary disagreed). Two consecutive clean runs is what was established, not a guarantee — these are model-dependent behaviours and a 14B model can regress on any one of them
    - **Honest scope of what "unmodified upstream" now means here.** The deployed image is upstream `v1.14.0` plus `c3b7967` (the `prompts/ask/*` grounding change, made during an earlier attempt at this task and kept) plus deploy configuration. Task 2.4's embed-failure report is **not** deployed: the host checkout sits at `c3b7967` and carries neither the `api/routers/sources.py` nor the `open_notebook/domain/notebook.py` change, which exist only in the workspace tree. So the 2.5 refusal result above is measured **with** the prompt change in place, and whether upstream refuses unaided was not established — the change was committed and deployed before this run began, and reverting it to find out was outside this task
    - **RESOLVED — see the watchdog note at the end of this task.** The paragraph below records the defect as first found; it is now detected and recovered automatically.
    - **Worker: it consumes reliably only while its LIVE query lives.** `surreal_commands` 1.3.1 scans `status = 'new'` exactly once at worker startup, then subscribes to a LIVE query on `command` with no reconnect, no polling fallback and no error path around the subscription itself. The SurrealDB restart five hours before this run had silenced it: four commands sat `new` for roughly two hours with nothing logged and the worker process healthy. Restarting `eeronotebook-app` drained them in under a second. Recovery is an app restart and **nothing detects the condition** — reported, not fixed, because a fix means either patching the library's listener or adding a queue-depth monitor, and neither is in this task
    - Chunk sizing confirmed by measurement, not by reading the default: inside the app container, against the live `source_embedding` rows, `CHUNK_SIZE=400 CHUNK_OVERLAP=60 MIN_CHUNK_SIZE=5` (all defaults, `OPEN_NOTEBOOK_CHUNK_SIZE` unset) produced 57 chunks whose largest was **393 tokens**, none over `CHUNK_SIZE` and none near `nomic-embed-text`'s 2048. Two of the three Sources genuinely required splitting, so this is not a small-input artefact. 3.5 codifies it
    - Cleanup: the previous attempt's `Worker live-query probe` Notebook and its Source are gone; the test's own teardown left nothing behind, so task 6.1 has no litter from tasks 3–4 to migrate. The queue holds no `new` and no `running` records. The one `running` record was marked `failed` by hand with its reason — its Source and Notebook no longer existed and nothing in the stack reconciles `running`. The 13 terminal `failed` records from earlier runs were left alone as a truthful log. Stack left up and healthy
    - **Requirement 2.5 does not hold under *partial* coverage, which this task did not test.** A later ad-hoc run asked one question mixing a covered fact with an uncovered one — the Vessey coefficient, present, and optimal study temperature, absent. The covered half was answered correctly from source; for the uncovered half the model supplied "70-75 degrees Fahrenheit" from its own knowledge, hedged with "not directly stated in the retrieved results", and placed a citation beside it. So the refusal instruction holds when *nothing* is covered and leaks when *something* is. The all-or-nothing case is what was verified above; the mixed case is open, and prompt wording alone has not secured it on a 14B model
    - **Watchdog: the silenced worker is now detected and recovered.** `scripts/worker_watchdog.py` runs as its own supervisord program — separate so it survives the failure it watches — reads the queue, and restarts the worker when a command stays unclaimed past a threshold. The restart is the point rather than a blunt instrument: the library already contains the recovery path in its startup scan and simply never runs it again, so restarting executes the code that drains the backlog instead of reaching into its listener. Needs a supervisord control socket, confined to a unix socket at mode 0700 and never TCP, since that interface is unauthenticated
    - **The first version could never have fired, and reproducing the fault is what found it.** It aged rows from a `created` column, and `command` has no timestamp at all — its fields are `app`, `args`, `context`, `error_message`, `id`, `name`, `result`, `status`. The library's own startup scan orders by that same absent column, which SurrealDB tolerates silently. Worse, the log line rendered the missing measurement as `0s`, so a detector that could not work read as a healthy queue. Unit tests passed throughout. `QueueState.describe()` now separates "first observation" from "0s" and a test asserts it
    - Persistence replaced age: an observer remembers which command ids it has seen unclaimed and for how long, which measures non-consumption directly and needs nothing from the library or the schema. Depth is explicitly not the signal — a busy worker legitimately has queued work, and treating that as a fault would restart a healthy worker under load
    - Proven against the real failure, not a simulation: restarting SurrealDB left work unclaimed, the watchdog tracked it 0s → 30s → 60s → 90s, fired, restarted the worker (uptime reset to 3s), the queue drained after 101s, and the Source reached `completed`. 18 tests cover the decision logic
    - **A Notebook delete left four orphaned Sources**, contradicting the cascade result recorded above. Observed on Sources attached at creation via the `notebook_id` form field rather than through the test's own path, so the two may differ; the Sources and their 70 embeddings had to be deleted individually. Worth resolving before task 6.1 migrates ownership, since an orphaned Source has no Notebook to inherit access from
    - _Requirements: 1.1, 1.2, 1.5, 2.1, 2.2, 2.3, 2.5_
  - [x] 3.5 Pin chunk sizing to the embedder's window
    - Closes the item 3.3 left open. Chunk size is now stated in configuration and held there by tests, rather than inherited from a default that happened to be safe
    - `OPEN_NOTEBOOK_CHUNK_SIZE=400` set in the host's `deploy/.env` (appended, mode 600 preserved, no other line read or touched) and in the committed `deploy/.env.example`. 400 is what 3.4 measured the stack already producing, so the pin changed no behaviour — the point is that changing it now has to be deliberate
    - **The `.env` entry on its own would have done nothing.** Compose reads `.env` for interpolation and injects none of it into containers, and the app service listed no chunking variable, so the pin also needed `OPEN_NOTEBOOK_CHUNK_SIZE: ${OPEN_NOTEBOOK_CHUNK_SIZE:-400}` in `deploy/docker-compose.yml`. A test asserts that passthrough exists and that its fallback is in range, because an environment file that reads correctly while the application runs on its default is exactly the failure this task exists to prevent
    - `open_notebook/utils/chunking.py` was deliberately left untouched. Its 8192 warning threshold is upstream's generic advice about "some embedding models"; 2048 belongs to `nomic-embed-text`, not to the chunker, so narrowing it there would put model-specific drift into a file upstream keeps changing, against Requirement 14.3. No startup guard either: an over-window chunk degrades retrieval, and a guard that refused to boot would trade that for an outage — a worse failure than the one it prevents
    - `tests/test_chunking.py` gained `TestChunkSizeWithinEmbeddingWindow`: six checks around a named `EMBEDDING_MODEL_MAX_INPUT_TOKENS = 2048` carrying its provenance — the resolved `CHUNK_SIZE`, overlap staying inside the chunk budget rather than adding to it, the pin in `.env.example`, the pin in the live `deploy/.env` when one is present (skipped off the Dev Server, since it is gitignored), the compose passthrough, and real chunking at three times the budget. Verified they bite rather than assuming: under `OPEN_NOTEBOOK_CHUNK_SIZE=4096` the configuration check fails and the behavioural one catches an actual 4094-token chunk
    - Mean pooling: **no over-window chunk can reach the embedder, but only because `CHUNK_SIZE` is inside the window.** `generate_embedding` embeds text at or under `CHUNK_SIZE` whole and never looks at it again — that branch contains no chunking at all, so its safety is precisely the value pinned above and nothing else. Above `CHUNK_SIZE` it splits, and every splitter path lands within budget, though HTML and Markdown split on headers rather than tokens and depend on `_apply_secondary_chunking` to come back under it. Now asserted rather than read: a test in `tests/test_embedding.py` captures every string handed to the model across all four content-type paths and fails if any exceeds the budget
    - `token_count` measures with tiktoken's `o200k_base`, not `nomic-embed-text`'s WordPiece vocabulary, so the budget is a proxy that reads low against what the embedder itself counts. At 400 the 5x headroom absorbs the error; a pin set at 2048 would not, which is why 2048 is a ceiling and not a target
    - Verified in the deployment and not only in the repo: app recreated, `OPEN_NOTEBOOK_CHUNK_SIZE=400` present in the container and `CHUNK_SIZE=400` resolved at import. A real ingestion through the API — one 5,600-word text Source — reached `completed` and `embedded`, producing 20 chunks whose largest was **385 tokens**, none over `CHUNK_SIZE` and none near 2048. `source_embedding` was empty beforehand, 3.4's data having been cleaned up, so this is the only measurement in the live database. Notebook deleted afterwards and both reads answer 404; `notebook`, `source`, `source_embedding` and `note` are all back to zero, so 6.1 inherits nothing from here
    - `uv run pytest tests/` is 658 passed and 18 skipped, up from 649 and 17; `ruff` and `mypy` clean. `yaml` needed an inline `import-untyped` ignore — it is a transitive dependency whose stubs exist on typeshed, which mypy reports regardless of `ignore_missing_imports`
    - **A concurrent actor redeployed the stack mid-task, and it cost the deploy configuration.** At 14:52 UTC the host checkout went clean at `04e49e6`, discarding the uncommitted `deploy/` edits it had been carrying, then rebuilt the image and recreated both containers. That took 4.1's `API_URL` and 4.2's `monitoring_net` on the gateway down with this task's passthrough: for twelve minutes the UI's TLS origin was wrong and the gateway monitor unreachable. Restored by copying the workspace's `deploy/` files back and running `docker compose up -d`, which recreated gateway and app without rebuilding — all three healthy, API `/health` 200, gateway back on `monitoring_net`. Nothing under `deploy/` is committed, so any actor with a clean checkout can undo it again; the durable fix is committing the deploy configuration, which is not this task's to do
    - Also changed under us: the running image is now built from `04e49e6` rather than `c3b7967` as 3.4 recorded, so the deployed code carries the later refusal-step change too
    - Left to the operator: raising the pin later means re-embedding every Source, since stored vectors keep whatever size they were built with, and restarting `eeronotebook-app`, since chunking constants are read at import
    - _Requirements: 1.3, 1.4_

- [x] 4. Bring the stack into operations
  - [x] 4.1 Route through the existing reverse proxy **(\*)**
    - `mon-traefik` runs the file provider only and reads no Docker labels, so the app registers as a file in its watched dynamic directory rather than through compose labels
    - `deploy/traefik/eeronotebook.yml.template`, rendered and installed by `setup-ingress.sh`; Traefik reloaded on its own, with no restart and no interruption to any other stack
    - `eeronotebook.local` → 8502 and `api.eeronotebook.local` → 5055, both over `websecure`, reached by container name on `monitoring_net`, so no host port was added and none could collide
    - Plain HTTP redirects with 301; the redirect is attached to the two eeroNotebook routers, not to the `web` entrypoint, because every other stack behind this proxy is HTTP-only
    - Private CA per the `EarWig/traefik/` pattern, generated on the host; the script adds a `serverAuth` EKU and an 825-day leaf, both of which modern clients require and that script predates
    - Registered as a `tls.certificates` entry, not a second default: `mon-traefik`'s dtefault certificate belongs to learninglab, and a second default would leave which one wins undefined — SNI selects instead
    - `API_URL` had to be set explicitly: left to auto-detect, the app told the browser to call `https://eeronotebook.local:5055`, a port the proxy neither serves nor terminates TLS on. Consequence is that the UI is now reached by name; `http://<host>:8502` remains only a debugging path
    - Verified on the host and again across the network: UI 307 → `/notebooks` 200, API health, and authenticated `/api/notebooks` 200 both directly and through the UI origin, all with the certificate verifying against the private CA and failing without it
    - Verified all eight pre-existing routes return their pre-change codes, and that learninglab's default certificate and its by-IP HTTPS fallback are untouched
    - **Not done, and not verifiable from here:** the `/etc/hosts` entry and the CA trust both need `sudo`, which this host does not grant without a password, and client devices are out of reach. Verification worked around both with `--resolve` and `--cacert`; `setup-ingress.sh` prints the two commands an operator must run
    - _Requirements: 13.5_
  - [x] 4.2 Monitoring and backups **(\*)**
    - Two monitors under a `📓 eeroNotebook Stack` group in `mon-uptime-kuma`: the API's `/health` and the gateway's `/health/liveliness` (Requirement 13.10), both `keyword` type so a 200 carrying the wrong body still fails
    - Both probe by container name over `monitoring_net`, as every other monitor on this host does — deliberately not `https://api.eeronotebook.local`, which has no DNS entry and a certificate from a CA nothing trusts yet, so a monitor there would alarm on task 4.1's outstanding operator steps rather than on the stack. A third monitor on the TLS front door is worth adding once those are done
    - Kuma 1.x has no API for creating monitors — only the socket.io channel the browser uses, which needs the admin login — so `deploy/monitoring/setup-monitors.sh` writes the rows into Kuma's own SQLite: copy `kuma.db` aside, stop the container, insert, start. Kuma reads its monitor list once at boot, so it cannot be done without the restart
    - `eeronotebook-inference` had to join `monitoring_net` to be reachable at all; it is still unpublished, and an unauthenticated `/v1/models` still answers `401`, so the added reachability grants nothing
    - Verified both report `200 - OK, keyword is found`, and that all 18 pre-existing monitors still report up after the Kuma restart
    - Verified down detection rather than assuming it: stopping the gateway moved the monitor to pending after ~30 s and to down at 2 m 45 s — interval 60 s plus two 60 s retries — and back to up within a minute of restarting it
    - Dozzle confirmed through its own log API, not by the presence of labels: 46, 40 and 38 real log messages returned for app, database and gateway. The `dozzle.enable` labels are in fact inert here — `DOZZLE_FILTER` is unset, so Dozzle shows every container regardless
    - Backups are `deploy/backup/`, scheduled as a launchd **user agent** at 03:30. A `LaunchDaemon` would need the `sudo` this host withholds, and would run before login, where OrbStack and therefore `docker` do not exist
    - Three artifacts per run, because the database is backed up twice on purpose: `db_data.tar.gz`, a byte-exact copy taken with SurrealDB stopped, which is the restore path of record; `database.surql.gz`, a hot logical export, which is the only thing that survives the rocksdb files being tied to the SurrealDB build that wrote them under a floating `:v2` tag; and `app_data.tar.gz`. A `MANIFEST` carries checksums, the SurrealDB version, and a fingerprint — not a copy — of the encryption key
    - `~/backups/eeronotebook` is `0700` and every artifact `0600`: the export contains the `credential` table
    - Two defects in SurrealDB 2.6.5's own export, both found by actually restoring what it produced. Exporting to stdout corrupts the artifact — the CLI writes its "exported successfully" log line into the same stream — so the export goes to a file inside the container and is copied out, and the script rejects any export containing CLI output. And SurrealDB cannot import its own export: `DEFINE FIELD in ON <relation table>` and `embedding[*]` collide with definitions the `DEFINE TABLE` already implied, so `restore.sh` rewrites every `DEFINE FIELD` to `DEFINE FIELD OVERWRITE`. Both are why the volume copy leads and the export follows
    - Verified the job runs through launchd, three times, exit 0, artifacts each time. The database was down 2–3 s and the application needed no restart — it opens a connection per query rather than holding a pool — and a database-backed API call answered `200` immediately after
    - **Restore proven, twice, without touching production:** artifacts extracted into scratch volumes and opened by a throwaway SurrealDB on `--network none`. All 19 tables came back at the live record count from *both* database artifacts (131 records), SurrealDB opened the restored rocksdb store directly, embeddings returned at 768 dimensions, the full-text index answered on restored data, and `app_data`'s four files were byte-identical to the live volume. Scratch containers and volumes torn down after
    - `restore.sh --production` is written but deliberately never run against live data: it takes a fresh safety backup first, requires typing `restore`, and stops the app and database while it replaces both volumes
    - **Left to the operator:** keep `OPEN_NOTEBOOK_ENCRYPTION_KEY` somewhere outside the backups, or a restored `credential` table decrypts to nothing. No notification channel is configured anywhere on this Kuma, so a red monitor currently tells nobody. And the backups sit on the same disk as the data they protect, so an off-host copy is still owed
    - Noticed and left alone: `mon-dozzle` is configured with `DOZZLE_REMOTE_AGENT=10.17.8.52:7007`, which fails every five seconds because `labplatform-containment` binds 7007 to loopback only. Pre-existing, and another stack's configuration
    - _Requirements: 13.6, 13.8_

- [x] 5. Identity
  - [x] 5.1 Add a dedicated GoTrue service
    - `eeronotebook-auth` (`public.ecr.aws/supabase/gotrue:v2.188.1`) and `eeronotebook-authdb` (`public.ecr.aws/supabase/postgres:17.6.1.084`) — **the same pairing as `lh-db`/`lh-auth` on this host**, both on `eeronotebook_internal` only.
    - **Started on `postgres:16-alpine` and abandoned it.** Supabase's GoTrue expects the `auth` schema, Supabase's role set, a `search_path`, and enum types its later migrations create; supplying those by hand took four attempts and each fix surfaced the next. Copying the pairing already working ten feet away was the correct move several attempts earlier. `deploy/auth-db-init/` existed for that bootstrap and is gone
    - Four wrong turns worth keeping, because each looked right: `POSTGRES_USER` decides both which role receives `POSTGRES_PASSWORD` *and* whether it can write the `auth` schema — in this image `postgres` is **not** the superuser, `supabase_admin` is, so the default yields `permission denied for schema auth`. `supabase_auth_admin` exists but never receives our password, so it cannot authenticate over the network. The image's `pg_hba` trusts `127.0.0.1` unconditionally, so a local `psql` password test **passes without checking the password** — that false positive is what made the role look usable. And `GOTRUE_JWT_ADMIN_ROLES` was absent, so no token counted as admin: with sign-up disabled, there was no way to create a member and nobody could ever have signed in
    - The application now carries `GOTRUE_JWT_SECRET`, because the boundary verifies tokens in that process. It is read only inside `open_notebook/identity/`, which a test enforces Verified: empty `PortBindings` on both, no host listener on 9999, and neither container on `monitoring_net` (Requirements 13.2, 13.9)
    - Deliberately separate from `lh-auth`: a shared auth database or JWT secret would make a compromise or migration in `legendary-hunts` reach this stack, and its settings are tuned for a different application — 6-character passwords against this stack's 12
    - `AUTH_DB_PASSWORD` and `GOTRUE_JWT_SECRET` generated on the host into `deploy/.env`, appended without reading or rewriting any existing line, mode 600 preserved. Verified compose fails fast: unsetting `AUTH_DB_PASSWORD` gives "required variable AUTH_DB_PASSWORD is missing a value" rather than a silent empty string. The DB password is hex on purpose — it is interpolated into a `postgres://` URL, where `@ : / ?` would terminate the URL early
    - **Sign-up disabled and verified, not assumed:** `POST /signup` answers `422 signup_disabled`. Members come from the operator via the admin path, which needs a `service_role` token task 5.2 mints
    - Access tokens expire in an hour with rotating refresh tokens. The shared password they replace never expired at all
    - `GOTRUE_MAILER_AUTOCONFIRM` is on because no SMTP exists on this host: a member required to confirm by email could never sign in. Revisit if self-service signup is ever enabled
    - **The `curl` check 5.1 asked for paid off:** the GoTrue image ships no `curl`, only `wget` and `nc`. Probed with `wget --spider`; Postgres with `pg_isready` against its own named database, since the default probe checks `postgres`, which is not the database GoTrue uses
    - **Three boot failures, each a prerequisite Supabase's own image supplies and a plain cluster does not**, and each visible only at the end of a full dump of the failing migration: `API_EXTERNAL_URL` is required and unprefixed, unlike every `GOTRUE_*` setting around it; the `auth` schema must already exist before the first migration's `CREATE TABLE IF NOT EXISTS auth.users`; and `20240612123726_enable_rls_update_grants` grants to `postgres` and `dashboard_user`, failing with `role "postgres" does not exist` on a cluster whose superuser is named otherwise
    - Fixed in `deploy/auth-db-init/01-auth-schema.sql` rather than by hand against the running database, so a fresh deployment works without a manual step. Since initdb scripts only re-run on an *empty* data directory, each discovery cost a volume wipe — which is why the role list is generous rather than minimal, all `NOLOGIN`, nothing authenticating as them. 23 `auth` tables migrated on the successful run
    - Nothing consumes this yet, by design: the application has no auth environment and no `depends_on` for it, verified by inspection, and stayed healthy throughout. Adding the dependency before anything reads it would only mean the app could not start when GoTrue could not
    - **Backup coverage added, and proven.** `deploy/backup/` now takes a schema-scoped `pg_dump` of the identity database — one artifact, not two, because `pg_dump` is Postgres's restore path of record while a data directory copy is locked to the server's major version and unsafe taken hot. `PGPASSWORD` is required even over the container's own socket; without it `pg_dump` prompts, writes nothing, and the result looks like an empty database rather than a refused connection, so the script now refuses an empty dump outright and records the member count in the `MANIFEST`. Verified by restoring into a throwaway Supabase Postgres on `--network none`: three members back with three password hashes, zero errors
    - Three beta members (`person1`–`person3`) exist for testing. Verified: sign-in returns an access and a refresh token, a wrong password gives 400, sign-up gives 422, a non-admin role is refused 403 by the admin API, and a real GoTrue token verifies through the boundary and resolves idempotently to one member. All seven token-tamper variants refused — an earlier "tampered token accepted" was an unsound shell test that appended `x` after stripping the last character, so a token ending in `x` was left untouched
    - _Requirements: 4.1, 13.2, 13.9_
  - [x] 5.2 Build the Identity_Boundary
    - `open_notebook/identity/` holds the `IdentityProvider` protocol, the GoTrue implementation, and one FastAPI dependency. A route depends on `current_member` and receives a `Member` — never a token, a claim set, or a provider name
    - HS256 verification with the secret read once at construction: GoTrue signs symmetrically, so there is no key material to fetch per request and nothing to refresh. A provider publishing a JWKS would cache differently behind the same protocol
    - Audience is always checked; **issuer only when one is expected**, because GoTrue omits `iss` unless configured and verifying an absent claim would reject every valid token. Both are tested
    - `open_notebook/domain/member.py` is the local identity everything else references, so application data carries a member id and never a provider's user id (Requirement 4.4). Provider-agnostic in prose as well as in code, and a test asserts the module names no provider — it caught the first draft, whose own docstring named GoTrue
    - **Migration 24 defines `member` with a UNIQUE index on (provider, subject), and that index is the concurrency guarantee, not the application code.** `Member.resolve` reads then writes, which is racy by construction; the index means the loser's insert fails and it re-reads, instead of both succeeding and leaving one person with two identities and two sets of Notebooks
    - Verified on a scratch SurrealDB on `--network none` before going near live data: the duplicate is refused with `already contains ['p', 's1']` and the count stays at 1, a different subject is accepted, the ASSERT refuses an empty subject, and the down path removes the table. Corrected an assumption while there — `SCHEMAFULL` **drops** an unknown field rather than rejecting the write, so it prevents field creation and the ASSERTs are what refuse bad values
    - `current_member` raises `AuthenticationError` and never returns `None`. 401 means "you are nobody" and an empty list means "you own nothing"; conflating them is how an access check passes while enforcing nothing. Task 6.2 depends on that distinction holding
    - Boundary enforced by test, not convention: `tests/test_identity_boundary.py` reads `open_notebook/`, `api/` and `commands/` and fails if any module outside the package imports a JWT library or reads a `GOTRUE_` variable. It also asserts the search found something, so it cannot pass vacuously. A stub provider satisfying the protocol resolves through the same seam untouched, which is the only mechanical check of Requirement 4.4 available
    - `pyjwt` is now declared in `pyproject.toml`. It was already installed transitively, which works until the parent that pulls it changes
    - **Migration numbering shifted:** the plan reserved 24 for 6.1, but 5.2 lands first and migrations are sequential, so owner scope becomes 25
    - Deployed and verified on the Dev Server: a backup was taken first, the live database moved 23 → 24 on API startup, `member` is queryable, all five containers healthy, API `/health` 200. 676 tests pass, `ruff` and `mypy` clean
    - **Task 2.4's embed-failure fix is now deployed** as a side effect — the host checkout had been pinned behind it, and this is the first deployment past that commit. Confirmed present in the running image
    - _Requirements: 4.2, 4.3, 4.4_
  - [x] 5.3 Replace the shared password
    - `MemberAuthMiddleware` resolves every request to a member or refuses it. `api/auth.py` and `PasswordAuthMiddleware` are deleted
    - A middleware rather than a global dependency, because it must refuse *before* routing. Division of labour is deliberate: the middleware answers who is calling, and routes answer what that member may reach by depending on `current_member` (task 6.2). `current_member` reuses what the middleware resolved, so a route and the gate can never disagree about the caller
    - **The admin password resolves to a member rather than bypassing resolution** (operator's decision). It maps to one `admin-password`/`operator` member, so task 6.2's ownership checks apply to the operator exactly as to anyone else — a credential that skipped resolution would skip every check built on it. Compared in constant time, as upstream did
    - **Authentication now fails closed.** Upstream skipped it entirely when no password was configured, so a deployment that forgot to set one served every notebook to anyone. There is deliberately no environment variable restoring that
    - The suite's several hundred route tests assert routing, not authentication, and would otherwise all assert 401. The only bypass lives in `tests/conftest.py` and requires running pytest; tests that must see the real gate opt out with `@pytest.mark.no_auth_bypass`. `tests/test_member_auth_middleware.py` adds 13 covering refusals, the fail-closed case, admin resolution, and the exclusion list
    - Exclusion list is upstream's verbatim and pinned by a test: `/api/config` and `/api/auth/status` are read by the frontend before sign-in, and the documentation routes are how an operator inspects a deployment that is refusing their credentials
    - A failed identity-store read answers **503**, never an empty list. "The database is down" must not look like "this member owns nothing", which is the shape that silently passes an access check
    - `GET /api/auth/status` now reports how to authenticate rather than whether a password is set. Exposes no secret: the method, that signup is closed, and whether an operator credential exists
    - Verified on the deployment: `/health`, `/api/config`, `/api/auth/status` answer 200 unauthenticated; `/api/notebooks` gives 401 with no header, with a bad token, and with a `Basic` scheme; 200 with the admin password and 200 with a real member token. Both identities exist as `member` rows — `admin-password/operator` and `gotrue/<uuid>`. 707 tests, `ruff` and `mypy` clean
    - **Found for 5.4:** `auth_url` currently reports `http://eeronotebook-auth:9999`, which no browser can reach — GoTrue is unpublished by design (Requirement 13.2). Sign-in must therefore be proxied by the app (`POST /api/auth/login` calling GoTrue server-side and returning the token) rather than the browser talking to GoTrue directly. Publishing GoTrue instead would trade a requirement away for convenience
    - _Requirements: 4.1, 4.5_
  - [x] 5.4 Move the frontend onto member sessions
    - **A sign-in proxy was a prerequisite nobody had planned for.** The provider is unpublished (Requirement 13.2), so the browser cannot reach it at all; the plan assumed the frontend would talk to GoTrue directly. The application performs the exchange instead. Publishing GoTrue would have traded a requirement for convenience and put an unauthenticated auth API on a shared network
    - `POST /api/auth/login` accepts a member's email and password, or the operator's password alone. The operator's is checked **locally and first**, so they can still sign in while the provider is down — which is exactly when somebody needs to. A bare password never reaches the provider, or the operator's credential would be sent to it
    - `POST /api/auth/refresh`, because access tokens last an hour where the shared password never expired; without it a member is signed out mid-session. `GET /api/auth/me` for the signed-in indicator
    - Provider error messages are **not** forwarded: they distinguish "no such user" from "wrong password", which is account enumeration for free. An unreachable provider answers 5xx, never 401 — telling a member their password is wrong while the service is down sends them resetting a password that was fine
    - `/status` no longer reports the provider's address, since it is unreachable from a browser and advertising it only invites a client to try
    - Verified on the deployment: member sign-in returns an access token, a refresh token and `expires_in` 3600; that token reaches `/api/notebooks` at 200; `/me` reports the right member; refresh rotates both tokens; a wrong password and an unknown address both give `401 Invalid credentials` with nothing distinguishing them; the operator signs in with the password alone as `is_admin` with no refresh token. 723 tests, `ruff` and `mypy` clean
    - Browser half done. `LoginForm` takes an email as well as a password, and an empty email means the operator. `auth-store` holds the refresh token and expiry, refreshes a minute ahead, and renews *before* testing the session rather than signing someone out for leaving a tab open. `checkAuth` now calls `/api/auth/me`, which also feeds the indicator
    - A refused refresh token ends the session; a **network** failure does not, so a blip no longer forces a fresh sign-in. That distinction is the difference between "your session is over" and "the wifi dropped"
    - The persisted key stays `auth-storage` and the access token stays at `state.token`, so `getAuthToken()` and the api client's interceptor are untouched, as the task required
    - The sidebar shows who is signed in beside the existing sign-out. With per-member notebooks, which member is looking is the difference between an empty list and a missing one
    - Errors carry a translation key rather than a hardcoded English string: the store lives outside React and cannot call `t()`, so it names the reason and the component translates it. Five new keys across **all fourteen** locales, since each `satisfies TranslationShape` and a missing key fails `tsc`
    - The locale parity test caught two mistakes of mine: three keys added without being wired to anything, and then a template literal that hid the key names from its scanner. Both fixed by referencing the keys literally
    - `npm run lint` (0 errors, 7 pre-existing warnings), `npm run test` 140 passed, `npm run build` clean, `tsc --noEmit` clean. Backend 723 passed
    - Verified through the app's own origin, as a browser would: member sign-in returns access and refresh tokens with `expires_in` 3600; `/api/auth/me` reports the right member; a refreshed token reaches `/api/notebooks` at 200; the operator path still returns `is_admin` with no refresh token; three members exist in the identity store
    - **Superseded 2026-09-24:** a member signing in through a real browser and landing on their notebooks has now been observed — see *Deployed* in 6.7. **Not verifiable from here, and unchanged:** the rendered DOM and the real browser session. `eeronotebook.local` resolves nowhere and the private CA is untrusted, both needing `sudo` on the Dev Server. `/login` answers 200 and the component and build are verified, but "a member types a password into a browser and lands on their notebooks" has not been observed
    - **Found, and a decision rather than a defect:** an old refresh token still returns 200 after GoTrue's `REUSE_INTERVAL` of 10s has passed — tested at 20s. A leaked refresh token therefore stays usable. Setting the interval to 0 makes reuse detection strict, but then two tabs refreshing at once can revoke the whole session, which in a classroom is a real cost. Left as configured and recorded, because the trade-off is the operator's to make
    - Keep the session under the existing `auth-storage` key so `apiClient`'s interceptor keeps working unchanged, and add refresh handling — access tokens expire where the password never did
    - Add sign-out, and surface the signed-in member in the layout
    - Every new string through `t('...')`, with keys added to all locales under `frontend/src/lib/locales/`; en-US is the reference and a missing key fails `tsc`
    - `npm run lint`, `npm run test`, `npm run build`
    - _Requirements: 4.1, 4.5_
  - [x] 5.5 Test the Identity_Boundary
    - Delivered with 5.2 rather than after it: `tests/test_identity_boundary.py` covers a valid token resolving to a member, and expired, wrong-signature, malformed, wrong-audience, wrong-issuer, missing-subject, `alg: none` and absent tokens each refused. All raise `AuthenticationError`, which `api/main.py`'s existing handler answers as 401
    - A stub provider satisfying the protocol resolves through the same seam with no change to the seam, and `IdentityProvider` is `runtime_checkable` so that is asserted mechanically rather than by inspection
    - A missing secret raises `ConfigurationError`, not `AuthenticationError`: nobody's credentials are wrong, the deployment is, and failing loudly beats verifying every token against an empty key
    - 18 tests. Optional in the plan, written anyway — the boundary's whole value is that it refuses things, and an unrefused token is invisible until it matters
    - _Requirements: 4.2, 4.3, 4.4, 4.5_
  - [x] 5.6 Checkpoint — identity
    - All six gates green: `uv run pytest tests/` 723 passed / 18 skipped, `ruff check .` clean, `mypy` clean across 144 source files, and the frontend's `npm run lint` (0 errors, the same 7 pre-existing warnings), `npm run test` 140 passed / 23 files, `npm run build` clean
    - `ruff` is not on `PATH` as a bare command in this environment; it runs as `uv run ruff check .`
    - **Also verified against the live deployment, which the checkpoint does not require but a demo does.** All five containers healthy; `tests/integration/test_baseline.py` ran 17/17 twice against `http://10.17.8.52:5055` — ingestion of all three Source kinds, embeddings, vector search, citations that resolve, refusal of an uncovered question, and cascade delete. So identity did not break the baseline that task 3.4 established
    - The two runs took 8m01s and 10m12s against 3.4's ~4m15s, and the cause is contention, not regression: they **overlapped**, and `OPEN_NOTEBOOK_WORKER_MAX_TASKS=1` serialises every ingestion in the deployment behind one worker. Worth knowing before several people upload at once
    - `member` was empty at the start of this check while GoTrue still held all three beta members, and that asymmetry is by design — `Member.resolve` recreates the local row on first sign-in, so the identity store is self-healing where the provider's user list is not. The admin password resolved to a fresh `member` row on the first authenticated call
    - The deployed checkout sits one commit behind `origin/eero` (`b658bf1` against `6e261a6`), and that commit touches only `tasks.md`, so the running code is current
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_

- [x] 6. Ownership and sharing
  - [x] 6.1 Add owner scope to the data model
    - Migration `25.surrealql` and `25_down.surrealql`, registered in `AsyncMigrationManager` at index 24 of both lists. `owner` on `notebook` is `option<record<member>>` with an index, plus the `share` relation, defined here rather than in 6.3 so 6.2 can enforce owner-or-Share from the outset. **Renumbered from 24** (5.2 took it), and taking 25 pushed 7.1 to 26 and 8.1 to 27 — both task bodies updated, since `tasks.md` had assigned 25 to 6.1 *and* 7.1
    - `owner` lives on `notebook` and nowhere else. Sources, notes and Study_Artifacts carry none of their own and inherit through the existing `reference` and `artifact` relations. One owner field is one place for an access check to consult; four would be four things to keep in agreement across an upstream merge, and a test asserts no second table gains one
    - **`owner` is optional, and that is a concession with a named cost.** A required field is the stronger guarantee — an unowned Notebook becomes unrepresentable — but SurrealDB does not rewrite existing rows when a field is defined: every Notebook predating the migration would become unwritable on its next UPDATE, and Notebook creation would fail outright until 6.2 wires an owner into the create path. The up path runs on API startup, so that is an app that will not boot. Optional instead, with the rule that `owner = NONE` means reachable by **nobody**. The dangerous reading of "inherit" is the opposite one, and `tests/test_owner_scope.py` exists to keep it from being written
    - Safe because `repo_update` issues `UPDATE $target MERGE $data`, not a replace, so `_prepare_save_data` dropping a `None` owner leaves the stored value alone. Were it a replace, renaming a Notebook would silently unown it — and an unowned Notebook is invisible to everyone. Pinned by a test, because it is a property of the repository layer that this design leans on rather than anything visible in the model
    - `role` on `share` is `ASSERT $value = 'viewer'`. That is Requirement 6.6 met structurally rather than by policy: there is no role to widen to, and a second one cannot exist without passing the ASSERT and a schema migration. A UNIQUE index on `(in, out)` gives one Share per member per Notebook — two would make revocation partial, deleting one while the other still granted access
    - Cascade events delete a Share when its Notebook or its member goes, at the database rather than in a route, so no delete path can forget. Verified: the share count went 1 → 0 on a notebook delete
    - **Verified up and down on a scratch SurrealDB 2.6.5 running the real `AsyncMigrationManager`, not a hand-written approximation of it** — 45 checks across an empty database and a populated one, all green. The populated case reproduced both carried findings: four orphaned Sources (3.4) and an *empty* `member` table (5.6). Down reverses the schema, destroys no content, and up runs again afterwards without wedging
    - Four SurrealQL behaviours the migration depends on were measured rather than assumed, and each could have broken it silently: `DEFINE FIELD` followed by an `UPDATE` using that field **does** work in one migration query; `WHERE owner IS NONE` **does** match rows created before the field existed; `IF … THEN … ELSE … END` works as a `LET` expression; and `array::first([])` is `NONE`. Also worth knowing for the next schema test: `INFO FOR DB` renders `FROM member TO notebook` back as `IN member OUT notebook`, which failed an assertion of mine that was wrong about the schema rather than the schema being wrong
    - **Requirement 7.5's owner is the admin member, created by the migration when it is absent.** 5.6's finding makes this necessary rather than tidy: `member` can be empty while the provider holds accounts, because `Member.resolve` recreates the local row on first sign-in, so the migration cannot assume an owner exists to point at. It creates `admin-password`/`operator` — the identity the shared password resolves to, and the honest answer to "who made this", since everything predating task 5 was created by whoever held that password. `Member.resolve` finds it by (provider, subject) afterwards and reuses it; migration 24's UNIQUE index makes a second one impossible. A test asserts the migration's literals match `ADMIN_PROVIDER` and `ADMIN_SUBJECT`, because if they drift the content is assigned to an identity nobody can sign in as and 7.5 is met in name only
    - Created **only when there is something to assign**, so installing from scratch manufactures no phantom owner — confirmed: 0 members and 0 notebooks after the migration on an empty database. The backfill `UPDATE` is guarded on `$admin != NONE` so it can never write a null over an owner, and there is no silent path: if the admin were needed and could not be created, the CREATE fails and the migration fails with it
    - **The orphaned Sources from 3.4: fail-closed, and the decision is to adopt them rather than delete or ignore them.** For access they are already fail-closed — an orphan resolves to no Notebook, so no check can grant it, and it is not a disclosure. The problem is at the other end: once 6.2 scopes every route through a Notebook, orphaned content can never be read or deleted through the application again, so leaving it means storage nobody can reach and nobody can clear. So the migration collects orphaned Sources *and* notes into one `Recovered content (migration 25)` Notebook owned by the admin. That **narrows** access — from anyone holding the shared password to the admin alone — and leaves the content visible enough for an operator to review. Deleting them was the alternative and was rejected: a migration that destroys content on startup is not a failure anyone can undo. The Notebook is created only when there is something to put in it, and a test forbids `DELETE source` / `DELETE note` appearing in the file
    - **Orphaning is ongoing, not historical, and that is the more useful half of 3.4's finding.** Measured directly: deleting a `notebook` record makes SurrealDB delete the `reference` and `artifact` edges itself while **keeping** the Source. So `Notebook.delete()` did not forget to clean up — its default `delete_exclusive_sources=False` deliberately keeps Sources, because upstream treats a Source as library content that may live in zero Notebooks. Migration 25 is therefore a one-time sweep and cannot prevent the next one. **For 6.2:** either deleting a Notebook takes its exclusive Sources with it, or orphans need a home, but the current default quietly produces unreachable content on every delete. This also explains 3.4's confusion about why its own cascade check passed while a hand-made Notebook left four Sources behind — the two paths differ in that flag
    - Notes are handled alongside Sources because `SourceInsight.save_as_note()` takes `notebook_id=None`, so a note with no Notebook is a reachable state and not corruption
    - Also found, recorded and deliberately **not** fixed: `ensure_record_id` is not idempotent for a numerically-keyed id — `member:9999` → `member:⟨9999⟩` → `member:⟨⟨9999\⟩⟩`, so such an owner would point somewhere new on every save. SurrealDB issues twenty-character alphanumeric keys, so an all-digit one is theoretical; the consequence is an owner pointing at a nonexistent record, which fails closed; and `ensure_record_id` is shared with every record link in the codebase, so changing it is its own task. Pinned by a test that fails if the escaping ever widens to the ids actually issued
    - `tests/test_owner_scope.py`, 32 checks over the schema text and the model field. Mutation-checked rather than assumed: widening `role` to include `editor`, dropping the `$admin != NONE` guard, and swapping orphan adoption for `DELETE source` each fail exactly the test meant to catch them. One check covers all 50 migration files rather than only 25 — `AsyncMigration.from_file` joins every line into one, so a comment after SQL on the same line would comment out every statement that follows, and nothing else would report it
    - Gates: `uv run pytest tests/` **759 passed / 18 skipped** (727 as of 2.4, plus these 32), `uv run ruff check .` clean, `uv run python -m mypy .` clean across 146 source files
    - **Superseded 2026-09-24 — applied and verified; see *Deployed* in 6.7, including why the ssh refusal below was never an access problem.** **Not applied to the Dev Server, and not verifiable from here.** `ssh` to the host is refused (publickey), so `deploy/backup/backup.sh` — which needs docker access to the `eeronotebook_db_data` volume — cannot run, and the live migration must not precede the backup. The API answers `/health` 200 but `/api/notebooks` 401, and `deploy/.env` is gitignored and absent from this workspace, so the live notebook, Source and note counts were **not** confirmed either: 2.4's "back to 0" is carried forward unverified rather than re-measured. Requirement 13.2 does still hold — the Dev Server's SurrealDB is unpublished, and the `200` on port 8000 is `labplatform-app` (uvicorn, no `/version`), not the database
    - **Left to the operator, in this order:** run `deploy/backup/backup.sh` on the host and confirm three artifacts plus a `MANIFEST`; update the host checkout and restart `eeronotebook-app`; watch the startup log for the version moving 24 → 25, because a failed up path stops the app. Then check whether a `Recovered content (migration 25)` Notebook appeared — if one did, the orphan sweep found pre-task-5 content that 2.4's counts said was gone, and what is inside it is worth reading before deleting. Do **not** reach for `restore.sh --production` casually; it has still never been run against live data
    - _Requirements: 5.1, 5.2, 5.3, 7.5_
  - [x] 6.2 Enforce ownership on every REST route
    - `open_notebook/domain/access.py`, 434 lines, and the only place a route learns what a member may reach. Shape: `Role` (`OWNER`/`VIEWER`), a frozen `NotebookAccess(notebook_id, role)` carrying `can_read` and `can_write`, and one database-backed decision — `notebook_access(member, notebook_id)` — that everything else composes. Around it: `accessible_notebook_ids` / `accessible_notebook_records` for list routes, and `require_{notebook,source,note,insight,chat_session}_{read,write}` for routes addressing one record. Routers call a one-liner; the resolution logic is all in the module
    - **Three database touchpoints, deliberately, and that is what made the test bypass tractable.** `notebook_access`, `accessible_notebook_ids` and `_notebook_ids_for` are the only functions that query; the `require_*` layer is pure composition over them. So conftest can neutralise enforcement for the existing route tests by patching three functions while leaving the composition — which role may do what — running as it does in production
    - Kept in `open_notebook/domain/` rather than split across `api/*_service.py`. The task text asked for both "one access module" and "logic in the service layer", and those pull apart: there is no `notebook_service.py` or `source_service.py` in this codebase (that logic lives in `open_notebook/domain/notebook.py`), so the domain layer *is* the service layer for this content. Routers stay thin either way, which is what the rule protects
    - **44 route-and-method pairs now resolve access, across 11 routers.** Counted by walking `app.routes` and checking each handler's dependency tree for `current_member`, not by reading the diff: notebooks 9, sources 12, notes 5, insights 3, chat 7, source_chat 6, embed 1, podcasts 1 (generate). `GET /api/auth/me` also depends on `current_member` but needs no access check — it returns the caller's own identity
    - Two new typed exceptions and two handlers in `api/main.py`. `AccessDeniedError` → 403 and `AccessUnavailableError` → 503. No access at all answers `NotFoundError` → 404, so a response never confirms a Notebook exists to somebody with neither ownership nor a Share; 403 is reserved for a Viewer refused a *write*, where hiding existence achieves nothing and a 404 would read as a defect. (Task 6.4 owns extending the 404 rule to Sources, notes and artifacts addressed by id, and to search.)
    - **The 503 rule from task 5.3 is enforced structurally, not by convention.** Every query in the module goes through one `_read()` wrapper that converts any failure into `AccessUnavailableError`. There is no path that returns `[]` on a failed read, and a test asserts `AccessUnavailableError` is a subclass of neither refusal, so a caller catching "denied" cannot swallow an outage. Mutation-checked: making `_read` return `[]` instead of raising fails four tests, including the route-level one that then answers `200 []` — an outage indistinguishable from owning nothing
    - Fail-closed on three separate things, each with its own test: `owner = NONE` is reachable by nobody (including by a Share pointing at it, which is logged and ignored — a Share adds to an owner's access rather than replacing it); a `Member` with no id reaches nothing and is never even queried for, since a query bound with `None` is where `owner = NONE` starts matching rows; and an id that `RecordID.parse` rejects answers "no access" rather than raising, so a malformed id is indistinguishable from a well-formed one that does not exist
    - `GET /api/notebooks`, `/api/sources`, `/api/notes` and `/api/recently-viewed` filter **inside** the query, binding `$accessible_notebooks`. Two indexed reads unioned in Python rather than one query with a graph traversal in its WHERE clause: each half is separately readable, and the fail-closed branch for a member with no id is visible in Python instead of buried in SurrealQL. Verified that `id IN []` matches nothing rather than everything, which is the one way this shape could have failed open
    - **The exclusive-source decision 6.1 handed over: a Notebook delete refuses to strand its Sources rather than silently deleting them.** `DELETE /api/notebooks/{id}` without `delete_exclusive_sources` now checks `get_delete_preview()` first and answers 400 naming the count and the two remedies when the Notebook holds Sources referenced nowhere else. Nothing is destroyed and nothing new is invented. The two alternatives were both worse: deleting them regardless of the flag destroys content a member explicitly asked to keep, which is exactly the failure migration 25 refused when it adopted orphans instead of deleting them; and adopting them into a per-member recovery Notebook works but invents product surface — an auto-created Notebook nobody asked for — on a decision this task should not make alone. `Notebook.delete()`'s own default stays at upstream's `False` (Requirement 14.3): the rule belongs at the API boundary where a member is choosing, not in a domain method background jobs also call
    - **Orphaning is closed at every path that could create it, which is the more useful half.** The refusal above covers a Notebook delete; `DELETE /api/notebooks/{id}/sources/{src}` refuses when it is the Source's last Notebook; `POST /api/notes` and `POST /api/sources` now *require* a Notebook (upstream allowed none — "allow sources without notebooks" is in `api/models.py` verbatim); and `POST /api/insights/{id}/save-as-note` requires one too, closing the `SourceInsight.save_as_note(notebook_id=None)` path 6.1 named. So the state migration 25 had to sweep can no longer be produced through the API
    - **An escalation that only exists once Shares do, closed at its entry.** `POST /api/notebooks/{nb}/sources/{src}` checks write on *both* ends. With a read-only check on the source, a Viewer could link a Source out of somebody else's Notebook into one of their own, become owner of a Notebook containing it, and then delete it. With write on both, only a member who already owns a Notebook holding the Source can link it, so a Source cross-referenced between two *different* members' Notebooks can no longer be created
    - Deleting content that spans Notebooks requires owning **every** Notebook holding it (`every_notebook=True` on `DELETE /api/sources/{id}` and `/api/notes/{id}`); editing in place requires owning one. Deleting removes it from all of them, so owning one is not permission to remove it from the others — whereas an edit only touches material the caller put there
    - **Verified against a real SurrealDB 2.6.5 running the real migration chain to version 25, not against mocks — 43 checks, all green.** Every query in the module plus the four scoped list queries, run verbatim including `get_sources`'s correlated `$parent.id` subqueries, which a changed FROM clause could have broken silently. Covered owner/Viewer/stranger/unsaved-member against an owned, a shared and an unowned Notebook; inheritance through `reference`, `artifact` and `refers_to` (both the Notebook-scoped and Source-scoped session shapes); an orphaned Source unreachable by its own creator; a bare id without its table prefix; the cross-Notebook delete refusal; and that the schema still rejects `role = 'editor'`. The scratch script and container are gone
    - That run found one defect, in the verification rather than the code: SurrealDB refuses `ORDER BY updated` when the projection is explicit and omits `updated` ("Missing order idiom"). The real `get_sources` projection does include it, so the route was fine and my check was wrong — but it is the trap to know about before narrowing any of these projections
    - `tests/test_access_control.py`, 49 checks, opting out of the bypass with `@pytest.mark.no_access_bypass`. Mutation-checked rather than assumed: reading `owner = NONE` as unrestricted fails 4 tests, answering a failed read as a denial fails 4, and dropping the single `require_notebook_read` call from `get_notebook` fails exactly 1 — the route-level check that exists because an access module nothing calls is worse than none, since it reads as protection
    - `tests/conftest.py` gained `authorized_requests` alongside 5.3's `authenticated_requests`, same autouse-plus-marker shape rather than a second mechanism. The existing route tests patch the symbols their own router imported, so the access module's reads would have opened real connections; three patches neutralise them. The test member also gained an id — `member:testsuite` — since an id-less member correctly reaches nothing
    - Four existing tests needed updating because the routes genuinely changed, not because the tests were wrong: two in `test_notes_api.py` created a note with no Notebook; `test_recently_viewed_api.py` asserted the exact bound params; and `test_typed_exceptions_reach_handlers.py` patched `Note.get_all`, which unfiltered `/api/notes` no longer calls
    - **Deliberately not scoped, deferred to 6.4:** `POST /api/search`, `/api/search/ask` and `/api/search/ask/simple`. 6.4 owns filtering inside `text_search`/`vector_search`'s SurrealQL and passing the member through the ask graph's event-loop workaround. Left untouched rather than half-done, and it is a live leak until 6.4 lands — search currently spans every member's Sources and notes
    - **Closed rather than left open:** `POST /api/commands/jobs`, `GET /api/commands/jobs` and `DELETE /api/commands/jobs/{id}` answer 403. The POST submitted *any* registered command with *any* arguments — `process_source`, `embed_source`, `run_transformation` — which is a write path into anybody's content around every check in this task. The list returns jobs with their `args` and `result`, and a `run_transformation` result is insight text. A command record carries no Notebook (`app`, `args`, `context`, `error_message`, `id`, `name`, `result`, `status`) so there is nothing to scope against, and every capability the POST reached has a scoped route of its own. The frontend uses none of the three
    - **Could not scope, and each is named because it stays open.** `GET /api/commands/jobs/{job_id}`: the frontend polls it after starting an insight, and a known id discloses that job's result; ids are twenty random characters rather than a sequence, so not enumerable. The six podcast-episode routes (`GET /podcasts/episodes`, `…/{id}`, `…/{id}/audio`, `POST …/{id}/retry`, `DELETE …/{id}`, `GET /podcasts/jobs/{id}`): `episode` is SCHEMAFULL with no Notebook and no owner, and an episode's `content` is a verbatim copy of the Notebook's Source text, so the list discloses other members' material — `POST /podcasts/generate` is gated on owning the named Notebook so no *new* episode can hold content its creator could not read, but the existing-episode routes need an owner on `episode`, which is a migration. Podcasts are a v1 Non-Goal and the live deployment has generated zero episodes (2.4 and 3.5 measured it), so current exposure is nil rather than small. `POST /api/embeddings/rebuild` and its status route: instance-wide by design, discloses nothing, rewrites vectors idempotently — a resource-consumption route any member can trigger, not an access leak, and confining it would need an operator role the design says does not exist. `GET /api/commands/registry/debug`: returns registered command names, no content
    - **55 further route-and-method pairs are instance configuration and carry no Notebook:** credentials 13, models 13, transformations 8, episode-profiles 6, speaker-profiles 6, settings 2, plus capabilities, languages, providers, config and the three auth routes. Any member can change them, which is real but is not Requirement 7.1 — that is about Notebook ownership and Share access. Worth a decision of its own: the design has no operator role, so "the member who configures the instance" has nowhere to live
    - **Found, and left for a later task to decide:** chat sessions on a Notebook are visible to everybody who can reach it, in both directions, because `chat_session` carries no member reference. So an owner sees a Viewer's questions and a Viewer sees the owner's. Consistent with the access model as specified — both parties have access to the Notebook, and the requirements reserve per-member privacy for Study_Progress only (9.5, 10.6) — but it is a privacy question somebody should answer knowingly. `chat_session` is SCHEMALESS (migration 3), so a member field could be added from application code with no migration. Meanwhile: creating and using a session needs read access, since asking questions is explicitly a Viewer's (Requirement 6.3), and renaming or deleting one needs ownership, since it would otherwise be renaming somebody else's
    - **One UI consequence, for 6.3 to absorb.** The delete dialog's "Unlink and keep them" radio now produces a 400 when the Notebook holds exclusive Sources, and `GET /notebooks/{id}/delete-preview` is owner-only so a Viewer opening the dialog gets 403. No frontend file was changed here — 6.3 owns the Notebook UI and needs to hide owner-only actions from a Viewer anyway, which is the same edit. The frontend already always sends a `notebook_id` when creating a note (`MessageActions` guards on it, `NoteEditorDialog` takes it as a required prop) and a notebook list when creating a Source, so those two new requirements break nothing
    - Gates: `uv run pytest tests/` **808 passed / 18 skipped** (759 as of 6.1, plus these 49), `uv run ruff check .` clean, `uv run python -m mypy .` clean across 148 source files
    - **Superseded 2026-09-24 — applied; see *Deployed* in 6.7.** **Not applied to the Dev Server.** Nothing here needs a migration, so the deployment is one checkout-and-restart away, but the behaviour changes are member-visible and worth landing alongside 6.3's Share UI rather than on their own — until Shares can be created, every Notebook is owner-only and a classroom has no path to a second reader
    - _Requirements: 7.1, 5.2, 5.4, 5.5_
  - [x] 6.3 Implement Shares
    - `api/routers/shares.py`, three routes nested under the Notebook they act on: `GET /api/notebooks/{id}/shares`, `POST` the same path with one address, and `DELETE /api/notebooks/{id}/shares/{member_id}`. Registered in `api/main.py` after the notebooks router. All three gated on `require_notebook_write` — 6.2's own function, so sharing is not a second access path — which makes a Viewer 403 and everybody else 404. Total route-and-method pairs resolving `current_member` is now 48, from 6.2's 45
    - **Listing is owner-only, not owner-and-Viewer.** A Viewer reading the list would learn the other Viewers' addresses, and the requirements only ever run visibility upward to the owner, never sideways between Viewers (Requirement 10.6's reasoning applied here). There is also no route that *edits* a Share: one role and revoke-only means an update endpoint would exist solely to carry a role the schema refuses, and a test asserts no PUT or PATCH on the path
    - `open_notebook/domain/share.py`, 300 lines and the only place a Share is written. Split from `access.py` deliberately: that module *consults* the relation on every request and this one *manages* it, so the read path — where a mistake is a leak — keeps exactly the shape 6.2 verified. `notebook_access` is untouched
    - **Requirement 6.6 is met three times over, all structurally.** `ShareCreate` has exactly one field and sets `extra="forbid"`, so a request carrying `members`, `emails`, `group`, `everyone` or `role` is **rejected with 422 rather than ignored** — which matters, because a plural field added to a client later cannot quietly do nothing until somebody adds it to the server too. No function in the module takes a collection and there is no plural form of `grant_viewer`. And `role` is the literal `'viewer'` inside the RELATE rather than a bound parameter, so no client value can reach it; the schema's ASSERT is the backstop, not the mechanism
    - **Naming a member: an address resolves against this instance's own `member` table, and never against the provider's accounts.** Recorded as [ADR-008](../../../docs/7-DEVELOPMENT/decisions/ADR-008-share-by-local-member.md) because it is a structural decision with a live cost. 5.6's finding is the whole problem: `Member.resolve` writes the local row on *first sign-in*, so an address can name a real account with no local record, and an owner sharing ahead of that first sign-in gets a 404. The message says "ask them to sign in once and then share again" rather than "no such person", because the lookup genuinely cannot tell those apart
    - **Asking the provider's admin API was the better answer and was rejected for this task.** It would work before a first sign-in and would return the provider's own `subject`, so the local row created then would be the row `Member.resolve` finds later — and it needs no new secret, since `GOTRUE_JWT_SECRET` is already held and a service-role token can be minted from it. Rejected because it could not be verified against a live provider from here, and an unverified call on the path that grants access to private material is a worse trade than a documented "sign in once first". Named in the ADR as the thing to revisit; it would change `member_for_email` and nothing else
    - **The tempting wrong answer, written down so nobody tries it:** creating a placeholder member from the address. `member` is keyed on (provider, subject) where subject is the provider's user id and never the address, so an invented row would never match the one created at first sign-in — the owner is told "shared" and the Viewer never gets in. A pending-invitation table was also rejected: new schema, and it makes a typo a grant to whoever registers that address next
    - **Account enumeration is weakened, knowingly and boundedly**, and 5.4's decision is the reason it needed weighing. A successful share confirms the address has signed in here at least once. Three things bound it: the caller is already an authenticated member the operator created by hand, not an anonymous visitor at a login form; a *refusal* is not an oracle for absence, since "no local row" covers both "no account" and "has one, never signed in"; and no endpoint lists members, so an owner can only test an address they already hold. A test asserts the refusal message contains neither "no such" nor "does not exist", and another asserts a Viewer is refused *before* any address lookup runs, so the endpoint is not an oracle on somebody else's Notebook
    - **Revocation deletes the grant and reaches nothing else.** One statement, `DELETE share WHERE in = $member AND out = $notebook RETURN BEFORE`; the test inspects the SQL rather than only its effect on a fixture, and fails if any content table appears in it. Access ends on the next request because `notebook_access` reads the relation per request and caches nothing — there is no session to expire. Revoking a Share that does not exist is 404 rather than a silent success: an owner told "revoked" when nothing was would stop looking
    - Granting twice is idempotent rather than an error — the UNIQUE index would reject the second RELATE, and two rows for one pair would make revocation partial. The same index handles the race: the loser re-reads, as `Member.resolve` does
    - **Every query in the module raises rather than answering empty**, extending 6.2's rule to management. "Nobody has access" is something an owner acts on, and an outage that looks like it would report every Share revoked; a failed address lookup answered as "no such member" would send an owner to ask a student to sign in again for no reason. Both answer 503
    - **`NotebookResponse` now carries the caller's `role`**, which is the UI consequence 6.2 handed over. Not the owner's identity — "what may I do here" is the only question a client needs answered, and a test asserts `owner` is not a response field. On the single-notebook read it comes from the access check that already ran (`NotebookAccess.granted_role`, which raises rather than fabricating `viewer` if the role is somehow absent); on the list it comes from `role_from_owner`, a pure function over the `owner` column the rows already carry, so list scoping stays at 6.2's two indexed reads instead of two more queries per row
    - **One narrowing in the list route, worth flagging.** A row whose role resolves to None is now dropped. That is not a new rule — an unowned Notebook is reachable by nobody, so `notebook_access` already 404s it — but 6.2's `accessible_notebook_ids` unions Shares without applying that rule, so a Share on an unowned Notebook could list a Notebook that 404s on open. With a `role` on the response the alternative was inventing one, and `viewer` would be a claim the UI acts on. Migration 25 backfills every unowned Notebook to the admin, so this is theoretical in practice
    - Frontend: `frontend/src/components/notebooks/NotebookShareDialog.tsx` plus `lib/api/shares.ts` and `lib/hooks/use-shares.ts` following the existing apiClient/TanStack shape. The dialog is **one address field, one button, no multi-select and no role picker** — Requirement 6.6 in the form's shape, with a visible line saying one person at a time. The list shows each Viewer with the grant's age and a revoke button behind a confirmation that states access ends and nothing is deleted. `useNotebookShares` takes an `enabled` flag because the route is owner-only and fetching it for a Viewer would 403 on every render. Errors show the **backend's own message** rather than a mapped generic one, since "ask them to sign in once" is the entire value of that response
    - **Owner-only actions are no longer offered to a Viewer**, threaded from `notebook.role` through one `canEdit` prop: the Notebook header (share, archive, delete, and in-place name/description editing become plain text, plus a "Shared with you" badge and a read-only notice), `NotebookCard` and `NotebookRow` (no actions menu at all rather than one whose every item 403s), `SourcesColumn` (no add-source, and `SourceCard` gained `canEdit` so remove/retry/refresh/delete are hidden rather than disabled — a greyed-out "Delete source" still reads as an offer), `NotesColumn` (no write-note, no per-note delete) and `NoteEditorDialog` (a `readOnly` mode that renders the note instead of an editor with a Save button that 403s). This also closes 6.2's handover: the delete dialog and its owner-only `delete-preview` are never reached by a Viewer, so the 403-on-open is gone
    - Chat is untouched for a Viewer because asking questions is explicitly theirs (Requirement 6.3). Only its two writes are withheld: `ChatPanel`'s `notebookId` prop is what surfaces "save as note", so it is left unset, and `SessionManager` gained `canEdit` to hide session rename and delete — creating and switching sessions still works, since 6.2 made those read-level
    - **Verified against a real SurrealDB 2.6.5 running the real migration chain to version 25 — 29 checks, all green**, not against mocks. Case-insensitive matching including a mixed-case stored address; the address-less admin member not breaking the lookup; the UNIQUE index rejecting a second grant; the schema still refusing `role = 'editor'`; `ORDER BY created` with `SELECT *`; a bare member id and an unparseable one; a revoke leaving Sources, notes and the Notebook in place; `notebook_access` returning VIEWER before and None after; and the cascade removing the remaining Share on a Notebook delete. Scratch container gone
    - **That run found the one real defect, and only a real database could have.** `_emails_for` bound member ids as strings, because `repo_query` renders every RecordID in a result as a string on the way out — so `id IN $members` matched nothing, **silently, as an empty result rather than an error**, and every Share came back with no address. The string form reads correctly everywhere else in the module, which is what made it easy to miss
    - Also measured rather than assumed: `string::lowercase(NONE)` does **not** fail the query in 2.6.5, so the `email != NONE` guard is defensive rather than load-bearing. Kept anyway — it makes an address-less member unmatchable by construction instead of by a coincidence of NONE propagation — and the measurement is recorded in the docstring so nobody re-derives it
    - `tests/test_shares.py`, 51 checks, opting out of the bypass with `no_access_bypass` so the real decisions run. One stand-in database behind both modules, so a test exercises the real sequence — access first, share management second — rather than passing with the ownership check answered by something the route never called. Mutation-checked: dropping `extra="forbid"` fails 3, gating the list on read instead of write fails exactly 1, letting the revoke statement name `source` fails 4, and reading an unowned Notebook as `viewer` fails 2
    - Frontend tests: `NotebookShareDialog.test.tsx` (7) and `NotebookHeader.test.tsx` (5). Mutation-checked too — hard-coding `isOwner = true` fails 3 header checks, and adding a second recipient field fails the 6.6 check. Used `fireEvent` rather than adding `@testing-library/user-event`, which is not a dependency of this project
    - Gates: `uv run pytest tests/` **859 passed / 18 skipped** (808 as of 6.2, plus these 51), `uv run ruff check .` clean, `uv run python -m mypy .` clean across 151 source files. Frontend `npm run lint` clean (7 pre-existing warnings, none new), `npm run test` **152 passed / 25 files** (140 as of 5.4, plus these 12), `npx tsc --noEmit` clean, `npm run build` succeeds. Markdown links check clean
    - i18n: 21 new keys under `notebooks` plus `sources.noSourcesViewer`, added to **all fourteen** locales — 22 per locale, inserted by a script anchored on two keys verified present exactly once in each file. Every key is referenced literally through `t('...')`, which the locale suite's unused-key scanner requires; 5.4's two recorded mistakes (keys wired to nothing, and a template literal hiding key names) were both avoided by writing the components first and deriving the key list from them
    - **Superseded 2026-09-24 — applied, and the Share path below walked in a real browser as both members; see *Deployed* in 6.7.** **Not applied to the Dev Server.** Nothing here needs a migration, so it remains one checkout-and-restart away — and 6.2's behaviour changes should land with it, as that task planned. Worth doing in this order once deployed: sign in as the operator, confirm a Notebook shows the Share button, share it with a member who **has already signed in at least once** (an address with no local row is refused by design), then sign in as that member and confirm the Notebook is listed with a "Shared with you" badge, no owner actions anywhere, and a working chat. Revoke, and confirm the Notebook disappears from their list on the next request while its Sources and notes are untouched
    - **Left open, deliberately.** The source-detail modal and the standalone `/sources` page still offer their own write actions to a Viewer who reaches a shared Source — insight generation, transformations, source editing — and answer 403. They are shared components reached from more than the Notebook page, and gating them is a sweep of its own rather than part of this task; 6.5's parameterised suite is where the gap becomes visible per route. Nothing there discloses content a Viewer cannot already read
    - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.6_
  - [x] 6.4 Scope search and stop disclosing existence
    - Closes the leak 6.2 named and left open — "search currently spans every member's Sources and notes". Measured before changing anything, on a two-member database: one unscoped text search returned **all 18 matching records**, including both members' sources and notes, an orphaned source, and a source in a Notebook owned by nobody
    - **The filter sits inside each branch of each search function, before that branch's LIMIT, and getting that wrong was the one real design decision here.** Recorded as [ADR-009](../../../docs/7-DEVELOPMENT/decisions/ADR-009-search-scoping-inside-the-query.md). Wrapping the `fn::` call in an outer `WHERE` needs no migration and looks equivalent; it is not. Both functions LIMIT their ranked output and `fn::vector_search` limits each of its three branches as well, so an outer filter narrows a top-N already chosen across every member's content. **Measured: with six of Bob's notes matching the same term, Alice asking for 3 results got 1 of her own 3 matches back.** The other two vanished with no error. A short result set is exactly what "no matches" looks like, which makes it the same invisible class of fault as a scoping filter that matches nothing
    - **Migration `26.surrealql` and `26_down.surrealql`**, registered in `AsyncMigrationManager` at index 25 of both lists. `fn::text_search` gains a fifth parameter and `fn::vector_search` a sixth, `$notebooks: array`; two `LET`s build the accessible source and note sets from `reference` and `artifact`, and every one of the nine content branches carries `IN $accessible_sources` or `IN $accessible_notes`. Everything else in both bodies is upstream's, unchanged, so a merge diff shows only the scoping — and relevance and similarity scores were compared against upstream's own definitions on the same rows and are **bit-identical**
    - **Taking 26 pushes 7.1 to 27 and 8.1 to 28; both task bodies are updated**, since 6.1 had already moved them once. Not taken lightly: staying out of the migration business was the alternative and it costs the correctness above
    - The changed arity is the backstop rather than a side effect. Upstream has redefined these two functions in migrations 1, 3, 4 and 9, so a future merge could plausibly redefine them again and remove the scoping. The application would then fail with `Incorrect arguments for function fn::text_search()` — loud, not silent — and `tests/test_search_scoping.py` fails at merge time before that, which is what Requirement 14.4 is for
    - **Going down fails closed, and that is worth knowing rather than assuming.** `26_down` restores upstream's definitions verbatim; the application still passes `$notebooks`, so every search then answers 500. Verified on a real database: down to 25 produced the arity error, not a widened search. A schema downgrade requires an application downgrade with it
    - `text_search` and `vector_search` take `member` as a **required keyword-only** argument with no default. An omitted member must not be able to mean "search everything": that failure returns *more* results, which reads as success. A caller that forgets gets a `TypeError`
    - **The access read happens before the `try` block, deliberately.** Everything inside becomes `DatabaseOperationError` → 500, and a failed access read has to stay `AccessUnavailableError` → 503. This is where the rule matters most in the whole API — an empty result list is indistinguishable from "no matches", so an outage answered as "you can see nothing" has no symptom at all. A test asserts the search query is never even issued when access is unknown, and another asserts `AccessUnavailableError` is not a subclass of `DatabaseOperationError`, since the router catches that one first
    - **The ask graph needed no event-loop workaround, and this task's own planning was wrong about that.** Every node in `open_notebook/graphs/ask.py` is already `async def` and `provide_answer` already awaited `vector_search`, so the member reaches an async call directly. `chat.py`'s `asyncio.new_event_loop()` / ThreadPool shape exists for its *sync* node; importing it here would have added a mechanism the backend rules call fragile to a path that never needs one. A test asserts every node is a coroutine function and that the module neither imports `asyncio` nor calls `new_event_loop` / `ThreadPoolExecutor` — checked over the parsed syntax tree, because the module's own prose explains why the workaround is absent and a substring scan matched the explanation
    - The member travels in `configurable`, alongside the three model ids the graph already receives that way. Config propagates to nodes dispatched with `Send`, so `trigger_queries` has nothing to copy and a search node added later cannot forget to — with graph *state* it would have been one more field to thread through each `Send` payload. `_asking_member()` refuses a run with no member, or one with no id, rather than searching every Notebook; checked at the entry node too, so the refusal costs no LLM call. Requirement 2.4 rests on this, and task 9.1 can prove it with disjoint seeded content
    - **Existence on id-addressed routes was already closed by 6.2; checked rather than redone.** Walked `app.routes`: all 64 id-addressed route/method pairs, and every one addressing a Notebook, Source, note, insight or chat session — 32 pairs — resolves through a `require_*` function that answers 404 for a member with no access. The remaining 32 are instance configuration (credentials, models, transformations, profiles) plus the routes named below. **Study_Artifacts do not exist yet**, so there is nothing to scope; task 8.1 inherits the rule and a search branch of its own — migration 26 has nine content branches and a `study_artifact` one would be the tenth
    - **`GET /commands/jobs/{job_id}` is closed, and without the migration 6.2 expected it to need.** 6.2 left it open because `command` carries no owner. An owner *column* was the wrong answer anyway: this application would have to write it after `submit_command()` returns, racing the worker already processing the row. Instead `CommandService.content_for_job` resolves a job back to the content it names — `source_id` / `note_id` / `notebook_id` in its args, every command in this codebase names its subject one of those ways — and falls back to the `source.command` link in reverse for `process_source`, which is submitted before its Source exists. Then 6.2's own `require_*_read` decides. A job naming no content is **refused**, so there is no "unscopable, therefore allowed" branch; a failed lookup raises rather than answering None. The frontend's only use of the route is polling after an insight, whose `create_insight` args carry `source_id`, so nothing breaks
    - Every refusal on that route is 404 with **one** message, whatever the reason — no such job, somebody else's source, nothing scopable. A test collects the three cases and fails if more than one distinct message comes back: a 403, or wording that differed per case, would confirm the job exists and hint at what it touched
    - **Still open, deliberately, and each for a stated reason.** The six podcast-episode routes need an owner on `episode` (SCHEMAFULL, no owner, and an episode's `content` is verbatim Source text) plus a product decision about who owns episodes predating the migration; podcasts are a v1 Non-Goal, the deployment has generated zero episodes, and putting that into migration 26 would have made one migration do two unrelated things. `GET /commands/registry/debug` returns registered command names and no content. `POST /api/embeddings/rebuild` and its status route are instance-wide by design and disclose nothing. A later task taking a migration for episodes starts at **29**, since 26, 27 and 28 are now spoken for
    - **Verified against a real SurrealDB 2.6.5 running the real migration chain to 26 — 24 checks, all green, twice on separate namespaces, and through the actual Python functions rather than raw SQL.** Owner, Viewer-by-Share, stranger and id-less member against an owned, a shared and an unowned Notebook; an orphaned Source unreachable by anyone; per-member result counts (2 against 16) proving `total_count` is derived from the caller's material only; a limit of 2 returning the caller's own 2 matches instead of being crowded out; the fail-closed 503 on both functions; and down-to-25-and-up-again. Scratch container and scripts removed afterwards
    - Two SurrealQL behaviours re-measured rather than carried forward: `out IN []` matches nothing rather than everything, and notebook ids bound as **strings** match nothing *silently* — 6.3's defect, reproduced here on purpose, which is why a test asserts the bound values are `RecordID` instances and not merely that a filter was applied
    - `tests/test_search_scoping.py`, 42 checks, opting out of the conftest bypass with `no_access_bypass`. Kept separate from `test_access_control.py` on purpose: that file covers the access *decision*, which is 6.2's and stable, while this covers where the decision is applied — an access check that is never consulted passes every test in the other file. The parameterised sweep over the whole route table is 6.5's and was not started
    - **Mutation-checked, and two of the first eleven mutations did not bite — both were fixed.** Dropping the scoping conjunct from *one* branch of a search function passed everything, because the test counted conjuncts in aggregate; it now checks each branch individually and names the binding that lost its filter. Worse, stripping `WHERE out IN $notebooks` from the two accessible-set lookups also passed: every branch still read `IN $accessible_sources` while those sets held the whole instance, so the filter was inert and looked correct. That has its own test now. The other nine bit first time: a default on `member`, the access read moved inside the `try`, ids bound as strings, the graph inventing a member, the job route answering 403, the job route allowing an unscopable job, the route dropping the member, and an outage answered as an empty result set
    - Four existing tests needed updating because the code genuinely changed, not because they were wrong: three in `test_search_api.py` now pass a member (and assert the highlight-overflow *fallback* carries it too, since a fallback that searched unscoped would turn a rendering bug into a disclosure); `test_owner_scope.py`'s exact migration count became `>= 25`, because that file is about migration 25 and its index-24 identity check is the real guard, so the newest migration's own test owns the exact count; and `test_typed_exceptions_reach_handlers.py`'s commands case gained a `prelude` that satisfies the new access check, or the 503 would have pre-empted the 422 the case exists to assert
    - No frontend change. Both search paths already send the member's token and neither request nor response shape moved. Route/method pairs resolving `current_member` measured at **51** by walking `app.routes` — the three search routes and the job route are new here. 6.3 reported 48 and 6.2 reported 44 and 45 of itself in different places, so that figure is a fresh measurement rather than a delta; spot-checked that no route lost a member, e.g. all 11 in the sources router still resolve one
    - Gates: `uv run pytest tests/` **901 passed / 18 skipped** (859 as of 6.3, plus these 42), `uv run ruff check .` clean, `uv run python -m mypy .` clean across 152 source files. mypy caught a real defect in my own test — `status.assert_not_awaited(), "message"` is a tuple expression, not an assertion, so that check had never run
    - **Superseded 2026-09-24 — migration 26 applied and search scoping verified live in both directions; see *Deployed* in 6.7.** **Not applied to the Dev Server, and unlike 6.2 and 6.3 this one needs a migration.** Operator order: run `deploy/backup/backup.sh` and confirm three artifacts plus a `MANIFEST`; update the host checkout; restart `eeronotebook-app` and watch the startup log for the version moving 25 → 26, since a failed up path stops the app. Then sign in as two different members and confirm each search returns only their own material. If the app boots but every search answers 500 with an arity error, the migration did not run and the fix is the migration, never widening the call back to four arguments
    - **Unresolved, for 6.5 or later.** `parent_id` on a text-search insight row is the insight's own id while the vector-search path uses the Source's — upstream's inconsistency, left alone under Requirement 14.3. It is not an access question (the insight branch is scoped through `source IN $accessible_sources`) but it will matter to anything that resolves a citation from a text-search result
    - _Requirements: 7.3, 7.4_
  - [x] 6.5 Build the access enforcement suite
    - `tests/test_access_enforcement.py`, 277 checks, opting out of the conftest access bypass with `no_access_bypass`. **Written to fail on a route that does not exist yet**, which is the whole point of the task: the route table is derived from `app.routes` at collection time and every method/path pair must land in exactly one named category, so a route added later lands in the category that requires scoping and fails by name. Proven rather than asserted — adding a plausible unscoped `GET /api/notebooks/{id}/summary` to the notebooks router failed four tests, three of them naming the route
    - **The route table is derived three ways, none of them by hand.** The pairs come from walking `app.routes` for `APIRoute` and expanding `methods - {HEAD, OPTIONS}`. Whether a route resolves a caller comes from `route.dependant.dependencies` — the dependency tree, not a `"current_member" in source` scan, which is what the three earlier tasks used. Whether it *scopes* comes from an AST call-closure over the handler, bounded to `api.*`: primitives are detected from any reference and delegation is followed only through real calls. Both halves were necessary. `commands.get_command_job_status` holds its checks in a dict and dispatches through it (`checks[kind](member, record_id)`), so a call-only scan reported that route as having no check at all; and `POST /api/sources/json` holds no check of its own, calling `create_source` directly, so a depth-zero scan would have reported it unscoped and the allowlist would have grown to hide it
    - **120 method/path pairs, and the partition accounts for every one.** 56 in eleven instance-configuration routers (allowlisted by module, with an exact-count assertion so a Notebook-touching route added to one of them fails rather than inheriting the carve-out — verified by adding `GET /api/settings/notebook-defaults/{id}`, which failed exactly that test); 2 public (`/`, `/health`); 3 closed to everybody (the command-job routes 6.2 shut); 9 known gaps; 1 that assigns ownership rather than checking it; **49 that must scope**. That 49 is 6.4's 51 member-resolving pairs minus `GET /api/auth/me` (own identity) and `POST /api/notebooks` (creation). The earlier counts — 44, 45, 48, 51 — are not carried anywhere in the file; nothing asserts a total, because the partition catches a new route without needing one
    - **The nine known gaps are an explicit allowlist with the reason attached, and each is checked to still *be* a gap.** The 6 podcast-episode routes (an episode's `content` is verbatim Source text; `episode` is SCHEMAFULL with no owner, so this needs the migration 6.4 said starts at 29, plus a decision about who owns pre-migration episodes); `GET /commands/registry/debug` (command names, no content); `POST /api/embeddings/rebuild` and its status route (instance-wide, discloses nothing). A gap that quietly acquires a scoping call fails `test_every_known_gap_is_still_a_gap` and tells the reader to delete the entry — verified by scoping one podcast route. So closing a gap later is a one-line deletion and opening one is a visible diff on a file whose whole subject is access
    - **6.3's frontend gap is now visible per route, and the finding is that it is cosmetic.** The six routes the source-detail modal and the `/sources` page offer a Viewer — source edit, delete, insight generation, retry, re-embed, insight delete — each answer 403, asserted individually and cross-checked to still be in the Viewer sweep. Nothing reachable, nothing disclosed; the backend is not what needs changing
    - **Requirement 6.3 needed its own sweep in the opposite direction, which the task text did not anticipate.** Five routes are writes by HTTP method that a Viewer is deliberately *allowed* — the chat session and message routes, because asking questions is explicitly a Viewer's. A method-shaped rule would have demanded 403 from all five; they are named in a `VIEWER_MAY` table with reasons instead, and asserted to get *past* the check, because a Share that refused everything would satisfy every other test in the file and grant nothing. Verified: tightening `POST /api/chat/execute` to a write check fails exactly that test
    - **The status code turned out to be insufficient evidence, and finding that out changed the design of every sweep.** Upstream wraps a failed `Source.get` as `NotFoundError`, so a route that skipped its access check entirely and then failed to read the record answers **404 — the same code a correct refusal gives**. So the stand-in database counts what was asked of it: a refusal must cost **zero** content reads, and the same request with only the access answer changed must differ in status, in content reads, or in the sequence of access questions. All three forms are needed — content routes differ in reads, the Share routes do their post-check work inside the `share` relation, and `GET /commands/jobs/{job_id}` differs only in status because its post-check read goes through the command library
    - Nothing in the file may reach a real database: `repository.db_connection` — the single choke point every `repo_*` goes through, including modules that imported `repo_query` by name — is replaced with one that counts the attempt and raises. Without it a handler that reads content before deciding access would pass against whatever sits in a developer's local SurrealDB, which is the one environment where every check appears to work
    - **Which layer supplies Requirement 7.4 differs by route, and only mutation revealed it.** Four of the five id-addressed content routes catch `NotFoundError` and re-raise one fixed message of their own, so the access module's wording never reaches the client and could drift there unnoticed; `GET /api/notebooks/{notebook_id}` passes it straight through. The existence tests are therefore parameterised over all five kinds rather than sampling one — the Notebook route catches drift in `access.py`, the other four catch a router that stops normalising. Each kind is refused for every reason it can be refused for (somebody else's, does not exist, in no Notebook at all) and all must read identically
    - `401` is swept with the real gate, which needed one piece of surgery worth knowing about: **Starlette binds `dispatch` when it constructs the middleware instance, once per process**, so by the time this file runs the shared stack has conftest's bypass bound into it and an unauthenticated sweep reads 503 from the blocked database rather than 401. The fixture rebuilds `app.middleware_stack` with the marker's patch off and restores the previous one. A companion check asserts the 401 carries `WWW-Authenticate: Bearer`, which only `MemberAuthMiddleware` sets — so the sweep is measuring the gate in front of routing rather than each route's own dependency
    - Also covered at the route level: a revoked Share ends access, driven through the real `share.revoke` against one mutable stand-in behind both modules, so the statement that runs is the real one and the access read that follows sees what it did — across all five content kinds at once, out of the member's list scope, and with no caching between two requests on the same client. And an access outage answers 503 on eight routes, with a separate assertion that no collection route answers 200 while access is unknown
    - Search is covered where 6.4 left off: at the route rather than the function. Both search types hand the caller down; the scope reaching the SurrealQL is the caller's own and is `[]` for a stranger; and the route does not filter afterwards (ADR-009). The four list routes are held to a stricter rule — **every** query a route issues must contain `IN $accessible_notebooks` and bind it to RecordIDs, not merely bind the parameter
    - **Mutation-checked: 18 mutations, 16 bit first time, and the two that did not were both the failure mode 6.4 warned about.** Sampling one route for the "one message per refusal" property passed, because Sources normalises in its own router — fixed by parameterising over all five kinds. And deleting `WHERE id IN $accessible_notebooks` while leaving the bind in place passed: the filter was inert and looked correct, which is 6.4's own finding reproduced in a new place — fixed by asserting the parameter appears in a filtering position in every query. The sixteen that bit: dropping the check from a read route (9 failures, named), from another read route (5), downgrading a write check to a read (exactly 1, named), widening `can_write` to a Viewer (25), answering no access as 403, unreadable content as 403, a failed access read as an empty list (8), one of two `recently-viewed` queries losing its filter, `text_search` binding no notebooks, a revoke reporting success without deleting (4), a new unscoped route, a new route in a configuration router, a gap acquiring a check, a route dropping `current_member`, the ask graph running without a member, a member-content path added to the authentication exemptions (7), the closed command-list route reopening, and a Viewer losing the right to ask questions
    - Three routes cannot be driven to an access decision and are named with the reason: `POST /api/sources` (multipart body; the same handler is driven as JSON through `/api/sources/json`, sharing its check) and the two ask routes (three model records and an embedding model are validated first). That set is asserted to be exactly those three, measured by **whether the access module was asked anything at all** rather than inferred from a status code — the first version inferred it from 400/422 and was wrong, because the ask routes answer *404* when their model lookup fails, which reads exactly like a refusal
    - **No route in the swept set is proved unscoped.** All 49 reach a scoping primitive and all 46 drivable ones refuse a stranger with 404 and a body carrying nothing but `detail`
    - Reused rather than duplicated: 6.2's decision suite keeps the `owner = NONE` and fail-closed rules and the created-notebook ownership check; this file adds only the premise that carve-out rests on — that **exactly one** route assigns an owner, and that it assigns `member.id`
    - Not repeated here, and still open from 6.4: `parent_id` on a text-search insight row is the insight's own id while vector search uses the Source's. Re-confirmed as not an access question — that branch is scoped through `source IN $accessible_sources` — so it stays with whoever resolves citations from text-search results
    - Deliberately not marked optional. Requirement 14.4 re-runs this after every upstream merge, and the design names access enforcement the highest-consequence divergence — an optional suite would be skipped exactly when it matters
    - Gates: `uv run pytest tests/` **1184 passed / 18 skipped** (901 as of 6.4, plus these 277 and 6.6's 6), `uv run ruff check .` clean, `uv run python -m mypy .` clean across 154 source files. mypy caught one real defect in the suite — `model.model_fields` on a bare `type` — which would have made the body synthesiser silently untyped
    - **Nothing to apply to the Dev Server.** No migration, no runtime code touched: this task added one test file and changed nothing under `api/` or `open_notebook/`. 6.2's, 6.3's and 6.4's deployments are still outstanding in the order those tasks recorded
    - _Requirements: 7.1, 7.3, 7.4, 5.4, 5.5, 6.4, 6.5_
  - [x] 6.6 Settle the MCP interface
    - **Decision: the MCP interface is not offered at v1, and Requirement 7.2 is satisfied structurally rather than by a second implementation.** Recorded as `docs/7-DEVELOPMENT/decisions/ADR-010-mcp-not-offered-at-v1.md`, indexed in the decision log. The premise held — no MCP server in this repository, no database access in the package, one enforcement point in `access.py` behind `MemberAuthMiddleware` — but the reason to decline is not enforcement, so "unavailable" needed a sharper statement than the task anticipated
    - **The tool list, checked against the route table rather than against the docs page.** Downloaded `open-notebook-mcp` 0.3.0 from PyPI (sha256 `4525535a…`) and read it: 33 tools, 32 issuing HTTP through one shared `make_request`, one (`search_capabilities`) a local catalogue. Walked `app.routes` against all 32 — **25 land on routes resolving `current_member`**, so they inherit 6.2 and 6.4 in full; 6 are instance configuration (`/api/models`, `/api/settings`); 1 (`get_model`) calls a route that does not exist here. None of the 32 paths is in `DEFAULT_EXCLUDED_PATHS`, so every tool passes the middleware
    - **Verified end-to-end against a locally-run API at migration 26, driving the real published package — 18/18 checks.** Scratch SurrealDB 2.6.5, the API's own startup migration chain, real middleware and real `require_*` gates. A stranger member got `count: 0` from `list_notebooks`, 404 from `get_notebook`/`get_note`/`update_*`/`delete_*`/`create_chat_session`/notebook-scoped `list_notes`/`list_sources`, and `total_count: 0` from `search` where the operator got 4 on the same query — so the query was real, not merely empty. The operator's note was byte-identical afterwards: none of the stranger's writes landed
    - **The reason it is declined: there is no durable way for an MCP client to hold a member identity.** The package puts `OPEN_NOTEBOOK_PASSWORD` verbatim into `Authorization: Bearer`, so 5.3 turned that into a general bearer slot with two fillings, and both were measured. A GoTrue access token authenticates as that member and scopes correctly — **but `GOTRUE_JWT_EXP` is 3600 and the package never calls `/api/auth/refresh`**; the value is fixed when the client spawns the process, and an expired token answers `Token has expired` on every tool. An hour of work then a human pasting a new JWT *is* the half-configured outcome the task named
    - **And the alternative is worse, which is why the docs had to warn rather than omit.** The documented configuration still works: measured `provider=admin-password, is_admin=true`, reading the seeded note in full. It is the only credential here that does not expire, and migration 25 backfilled every previously-unowned Notebook to the admin — so following the existing page hands a third-party client, and every assistant conversation through it, the whole library. Silence would have left that discoverable and unflagged
    - Docs: `ADR-010` holds the reasoning and everything measured. `docs/5-CONFIGURATION/mcp-integration.md` gains a status notice **above** the instructions a reader would copy, plus an inline correction where it calls the variable an optional password — it is neither optional nor a password field here. `docs/7-DEVELOPMENT/security.md`'s Authentication section still claimed `PasswordAuthMiddleware`; corrected with the single-enforcement-point statement and pointed at ADR-010, since a 7.2 auditor starting there would otherwise conclude the codebase predates 5.3. Upstream's body is kept and labelled on both pages rather than deleted, to keep the merge surface small (Requirement 14.3); `README.md` is untouched for the same reason, and its link now lands on the notice
    - `tests/test_mcp_interface_absent.py`, 6 checks, deliberately cheap: no first-party module imports an MCP server framework, `pyproject.toml` declares no MCP dependency or script, the page carries its status above the upstream body, and ADR-010 exists and is indexed. It guards the *decision*, not the enforcement — 6.2/6.4 measured that and 6.5's sweep is the version-independent guarantee. **Deliberately not pinned: a frozen copy of a third-party tool list**, which would rot on their next release and read as reassurance. Mutation-checked: an `mcp` import added to `api/middleware.py` fails it, and softening the notice fails it
    - **Superseded 2026-09-24 in part:** the ssh refusal below was the bare IP, not access — see *Deployed* in 6.7. The MCP-as-member run itself has still not been repeated against the deployment. **Not verifiable from here, and stated rather than glossed:** "point it at the deployed API as a member with no access" could not be run. `ssh` to the Dev Server is still refused (publickey) and `deploy/.env` is gitignored and absent, so no credential for the deployment exists in this environment. What *was* run against it: `/api/auth/status` reports `method=member_token` with `admin_password_configured=true`, so 5.3/5.4 are live there, and the real MCP server pointed at `10.17.8.52:5055` had all six read tools refused under no credential (`Missing authorization header`), a wrong password and a forged token (`Token is not valid`). That proves the deployment refuses an unresolvable caller; it does **not** prove member scoping there, because 6.2/6.3/6.4 are still unapplied
    - **Found, not fixed, and a decision for someone else:** `get_settings`, `update_settings`, `list_models`, `create_model` and `get_default_models` all succeeded for a member with no Notebook access — measured, and consistent with 6.2, which scoped content routes and left instance configuration shared. Not a disclosure (no member content in either payload) but two of them are writes to shared configuration by anyone who can authenticate. Recorded in ADR-010's consequences
    - Also found: 0.3.0 has drifted from this API independently of access — `get_model` 405s on a `GET /api/models/{id}` that does not exist, and `execute_chat` and `get_chat_context` omit `context`/`context_config` that this API's schemas require (422). One more reason not to make the interface a v1 commitment
    - Gates: `uv run pytest tests/` **907 passed / 18 skipped** (901 as of 6.4, plus these 6), `uv run ruff check .` clean, `uv run python -m mypy .` clean across 154 source files, `scripts/check_md_links.py` clean. **All three excluded 6.5's `tests/test_access_enforcement.py` and its two scratch files, which are mid-edit in a concurrent session** — that test file does not yet import cleanly, and ruff and mypy each flag it. Nothing here touches them, and the exclusions are named rather than folded into a clean claim. Scratch container, scratch scripts and the extracted sdist removed
    - _Requirements: 7.2_
  - [x] 6.7 Checkpoint — access control
    - **All eight gates green in one run with nothing excluded:** `uv run pytest tests/` **1184 passed / 18 skipped**, `uv run ruff check .` clean, `uv run python -m mypy .` clean across 154 source files, and the frontend's `npm run lint` (0 errors, the same 7 pre-existing warnings 5.6 recorded), `npm run test` **152 passed / 25 files**, `npm run build` clean, `npx tsc --noEmit` clean, and `scripts/check_md_links.py` reporting every relative link resolving. 5.6 ran six; `tsc` and the link check are added here because 6.6 ran the latter and 6.3 added frontend files
    - **The combined 6.5/6.6 state is clean, which is the thing this checkpoint existed to establish.** Those two ran concurrently and 6.6's gates excluded `tests/test_access_enforcement.py` plus two scratch files, so neither task had ever measured the whole tree. 1184 is exactly 6.5's figure and it accounts for 6.6's six (901 as of 6.4, plus 277, plus 6), so nothing was lost between them. `test_access_enforcement.py` alone is 277 passed and `test_mcp_interface_absent.py` 6 passed; ruff and mypy ran over the whole tree with no exclusion, which is the same claim from the other direction
    - **6.6's two scratch files are gone — checked rather than assumed.** Every untracked or ignored path in the tree is a named deliverable of 6.2–6.6: `api/routers/shares.py`, `open_notebook/domain/{access,share}.py`, migration 26 up and down, five test files, the three ADRs, and the frontend Share components. No scratch `.py` exists anywhere outside `.venv`, `node_modules` and the caches, and the scratch SurrealDB containers from 6.1–6.6 are all stopped with none running
    - Coherence spot-check 1 — **the known-gap allowlist matches what 6.2 and 6.4 actually left open, exactly.** `KNOWN_GAPS` holds the six podcast-episode routes (`GET /api/podcasts/episodes`, `…/{id}`, `…/{id}/audio`, `POST …/{id}/retry`, `DELETE …/{id}`, `GET /api/podcasts/jobs/{id}`), `GET /api/commands/registry/debug`, `POST /api/embeddings/rebuild` and `GET /api/embeddings/rebuild/{id}/status`. That is 6.2's unscopable list minus `GET /api/commands/jobs/{job_id}`, which 6.4 closed through `content_for_job` — so the only entry that moved is the one a later task genuinely shut, and nothing was dropped quietly
    - Coherence spot-check 2 — **migration numbering holds end to end.** 26 up files and 26 down files, 1–26 contiguous; `AsyncMigrationManager` carries 26 entries in each list, both ascending, no duplicate, no gap, and the up set equals the down set. `tasks.md` reads 7.1 → 27 and 8.1 → 28, and 6.4's "a later episode migration starts at 29" is consistent with both
    - Coherence spot-check 3 — **ADR-008, ADR-009 and ADR-010 exist and are all indexed**, at lines 52–54 of `docs/7-DEVELOPMENT/decisions/README.md`, each marked Accepted. Worth knowing about the link gate: `check_md_links.py` walks only *tracked* markdown and the three ADR files are still untracked, so it proves the index points at files present on disk rather than checking the ADRs' own links
    - **Found here, and it invalidates a step both 6.1 and 6.4 recorded: there is nothing on `origin/eero` to deploy.** `origin/eero` sits at `6e261a6` and its newest migration is 24. 6.1 is committed locally as `ccb7b10` and unpushed, and **6.2 through 6.6 are uncommitted working-tree changes** — 54 modified files and 18 new ones beyond this spec's own, including migration 26, `access.py`, `share.py` and the shares router. So "update the host checkout" fetches nothing today: an operator following either task's list in order would restart the app onto unchanged code, see the schema version stay at 24, and reasonably conclude the migration had failed. Left to a human deliberately — it spans five tasks' work, and deciding how to commit that is not a checkpoint's call
    - **3.5's worry about the deploy configuration is resolved, which lowers the risk it named.** All 13 files under `deploy/` are tracked and `HEAD` matches `origin/eero` for that directory, including 3.5's own `OPEN_NOTEBOOK_CHUNK_SIZE: ${OPEN_NOTEBOOK_CHUNK_SIZE:-400}` passthrough. A concurrent actor with a clean checkout can no longer discard the deploy config the way one did during 3.5. `deploy/.env` is still gitignored and host-only, so it stays the one thing a clean checkout cannot restore
    - **Superseded 2026-09-24 — deployed from this environment; see *Deployed* below.** **Still not deployable from this environment, re-confirmed rather than carried forward:** `ssh 10.17.8.52` is refused (`publickey,password,keyboard-interactive`) and `deploy/.env` is absent here, so `deploy/backup/backup.sh` cannot run and no live migration was attempted. The deployment is alive and still on pre-6.2 code — `/health` 200, `/api/notebooks` 401, and `/api/auth/status` reporting `method=member_token` with `admin_password_configured=true`, so 5.3 and 5.4 are live there and 6.1 through 6.6 are not
    - **The deployment backlog for 6.1–6.4, consolidated into one order so it is not spread across four task bodies.** Each step names the task it comes from; nothing below was run:
      1. **Commit and push.** None of the four tasks recorded this and every later step depends on it: commit 6.2–6.6's working-tree changes and push `eero`, so `origin/eero` carries migrations 25 and 26 and the access code
      2. **Back up before anything touches the database.** Run `deploy/backup/backup.sh` on the host and confirm `db_data.tar.gz`, `database.surql.gz`, `app_data.tar.gz` and a `MANIFEST` (6.1, 6.4 — both asked for it; once covers both migrations if they land in one restart)
      3. **Update the host checkout and restart `eeronotebook-app`** (6.1, 6.2, 6.3, 6.4). Afterwards confirm `deploy/.env` is still present and mode 600 — it is gitignored, so a checkout cannot restore it
      4. **Watch the startup log for the schema version moving 24 → 25 → 26** (6.1, 6.4). A failed up path stops the app. Deploying both migrations together means one restart crossing two versions rather than the two separate steps those tasks wrote
      5. **Read any `Recovered content (migration 25)` Notebook before deleting it** (6.1). If one appeared, the orphan sweep found pre-task-5 content that 2.4's counts said was gone
      6. **Confirm search scoping:** sign in as two different members and confirm each search returns only their own material (6.4). If the app boots but every search answers 500 with an arity error, the migration did not run — the fix is the migration, never widening the call back to four arguments
      7. **Walk the Share path in this order** (6.3): as the operator, confirm a Notebook shows the Share button; share it with a member who has **already signed in at least once**, since an address with no local row is refused by design (ADR-008); sign in as that member and confirm the Notebook is listed with a "Shared with you" badge, no owner actions anywhere, and a working chat; revoke, and confirm it leaves their list on the next request while its Sources and notes stay untouched
      8. **Do not run `restore.sh --production`.** It has still never been run against live data (4.2); step 2's backup is the thing to fall back to
      - **Deployed 2026-09-24; all eight steps above done in order.** (1) Committed as `d9c940e` (6.2–6.5), `905e161` (6.6) and `e67d890` (this log), gates green before and after: 1184 passed / 18 skipped, ruff, mypy over 154 files, and the five frontend gates. (2) Backup `~/backups/eeronotebook/20260924-083624`: all four artifacts plus a `MANIFEST`, SurrealDB down 2 s, 3 identity members captured. (3) Host checkout `b658bf1` → `e67d890` by fast-forward, `deploy/.env` intact at mode 600, image rebuilt. (4) **24 → 25 → 26 at 14:39:49 UTC**, both recorded in `_sbl_migrations`, all five containers healthy. Schema read back rather than assumed: `owner` is `option<record<member>>` with its index, `share.role` carries the `viewer` ASSERT and a UNIQUE index on (in, out), and both search functions take `$notebooks` and use it inside every branch. (5) No `Recovered content (migration 25)` Notebook, correctly: the content tables were empty beforehand — 0 notebooks, sources, notes and embeddings, 3 members — so the backfill and the orphan sweep both ran as no-ops, and **neither path has yet been exercised against real data**. (6, 7) Two demo members seeded through the operator's admin path; `scripts/demo_seed_and_verify.py` passed **22/22 live**: scoped lists both ways, 404 rather than 403 for no access, search scoped in both directions, a Share granting `viewer`, a Viewer's rename and delete refused 403, and revocation ending access without touching content. The Share path was then walked in a real browser as both members. (8) `restore.sh --production` not run
      - **Why four task bodies recorded the host as unreachable, when it never was.** 6.1, 6.4, 6.6 and this task connected to `10.17.8.52` directly. The identity file is bound to the `labserver` alias in `~/.ssh/config`, and a bare IP matches no `Host` block, so no key was ever offered. `ssh labserver` works. The deployment backlog was deferred to a human on the strength of a typo-shaped failure
      - **Found by using a browser, and fixed.** Every check in 6.2–6.5 went straight to the API, so none of these could appear there. **(a)** Signing out as one member and in as another in the same tab rendered the first member's notebook list until a reload: TanStack Query keys such as `['notebooks']` carry no member id, and `logout()` cleared the session but not the cache. Fixed in `268024e` by clearing it on logout *and* on a successful sign-in; 4 tests, mutation-checked. **(b)** 4.1's `API_URL=https://eeronotebook.local` sends the browser to a name no client can resolve, so the UI did not work for anyone; made overridable in `c467019` and set to `http://${EERONOTEBOOK_HOST}:5055` in the host's `deploy/.env`. Not `:8502`: the `/api/*` rewrite is a Next.js proxy with a 30 s default timeout, measured failing a ~60 s synchronous call — reasoning corrected in `deploy/docker-compose.yml`, since the first version named the wrong call. **(c)** The UI called itself Open Notebook; rebranded in `1fd7f86`, deliberately keeping upstream's name on the two strings that describe upstream (the documentation link and the release check), with a test pinning both directions
      - Also closed here: 5.4's "a member types a password into a browser and lands on their notebooks" has now been observed, as has 6.3's Viewer UI — the "Shared with you" badge, no actions menu, a read-only notice, and no add-source control. A full sweep of every route as a signed-in member found no uncaught exception, no console error and no response above 399. The operator has since exercised the deployment by hand with no server error logged
      - 6.2 needs no step of its own — no migration, and its member-visible changes land with 6.3's Share UI, which step 7 exercises. 6.5 added one test file and 6.6 only docs, so neither has anything to deploy. 4.1's two `sudo` steps (the `/etc/hosts` entry and trusting the private CA) are independent of this list and also still outstanding
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 7.1, 7.2, 7.3, 7.4, 7.5_

- [ ] 7. Study progress
  - [~] 7.1 Model Study_Progress per member
    - Flashcard scheduling state and quiz attempts keyed to member and Study_Artifact, stored separately from the artifact
    - Migration `27.surrealql` and its down file, plus the `AsyncMigrationManager` entry. **Renumbered twice, now to 27:** 6.1 took 25 for owner scope and the `share` relation, and 6.4 took 26 to put notebook scoping inside the search functions. Migrations are sequential
    - Domain model in a new `open_notebook/domain/study.py`; every read and write filters on the resolved member, with no code path that takes a member id from the request body
    - Deleting a Share leaves progress rows untouched; deleting the artifact is the only thing that removes them **(\*)**
    - No endpoint returns another member's progress, and none exposes an aggregate across members — an average over a class of two identifies both **(\*)**
    - _Requirements: 9.3, 9.4, 10.2, 10.4_
  - [~] 7.2 Test progress isolation
    - Two members on one shared artifact: each sees only their own state, and one member's review does not change the other's next due card
    - Revoking a Share leaves the revoked member's rows present, and readable again if access is restored
    - _Requirements: 9.3, 9.4, 10.2, 10.4_

- [ ] 8. Study artifacts
  - [~] 8.1 Structured generation
    - Generate against `qwen2.5:14b` using tool calling to constrain output to a schema; do not parse free text
    - Validate every response against the schema, retry on failure, and never persist an invalid artifact
    - Record the Sources each Study_Artifact was generated from
    - Migration `28.surrealql` and its down file for `study_artifact`, plus the `AsyncMigrationManager` entry; the record holds its Notebook, its type, the Sources it came from, and the validated payload. **Renumbered twice, now to 28:** 6.1 took 25, 6.4 took 26 for search scoping and 7.1 took 27, and migrations are sequential
    - Reach the model through `provision_langchain_model()` like every other LLM call, never a provider client directly, and bind a Pydantic schema as a tool. The gateway serves the logical name `eero-synthesis`, so nothing here names `qwen2.5:14b`
    - Validation is not a separable step from persistence: the schema check is the decision to write. Raise `ValueError` for a permanent failure so the `stop_on` blocklist in `commands/` stops retrying, and anything else to let it retry
    - Run generation as a background command in `commands/study_commands.py`, submitted fire-and-forget; the worker must be running or nothing happens and nothing complains
    - Wrap the call with `classify_error()` and strip thinking output with `clean_thinking_content()`, following the existing graph nodes
    - Surface a failed generation against the request the way 2.4 surfaced a failed embedding — a fire-and-forget job that fails invisibly is the defect that task already paid for once
    - _Requirements: 8.1, 8.2, 8.3, 8.4_
  - [~] 8.2 Flashcards
    - Deck generation from a Notebook's Sources, with review scheduling driven by the per-member state from task 7
    - Card schema carrying front, back, and the Source it came from, so a card remains traceable like a citation
    - Routes to generate a deck, fetch the calling member's due cards, and record a review; the deck itself holds no schedule
    - Frontend for review under a new `frontend/src/components/study/`, with i18n keys in every locale
    - _Requirements: 9.1, 9.2_
  - [~] 8.3 Quizzes
    - Question generation with answers and explanations; attempts and scores recorded per member and visible to that member
    - Schema requires an answer and an explanation per question, so an unexplained answer fails validation rather than reaching a member
    - Routes to generate a quiz, submit an attempt, and list the calling member's own attempts
    - Frontend and i18n as for flashcards
    - _Requirements: 10.1, 10.3_
  - [~] 8.4 Mind maps
    - Hierarchical concept structure extracted from Sources, persisted as data rather than a rendered image so it stays navigable and can expand and collapse
    - Bound depth and node count in the schema; an unbounded graph from a 14B model is unrenderable and there is no way to recover it after the fact
    - Frontend renders expand and collapse from the stored structure, with i18n
    - _Requirements: 11.1, 11.2, 11.3_
  - [~] 8.5 Text artifacts as configuration
    - Study guide, briefing document, FAQ, and timeline as transformation prompt templates, retained as notes in the originating Notebook
    - No application code; this is the cheap half of NotebookLM parity, and the clearest instance of preferring configuration over divergence
    - Templates go under `prompts/transformation/` and are registered as transformations; retention as notes is upstream behaviour already
    - Templates are cached, so restart the app after editing one
    - _Requirements: 12.1, 12.2, 12.3, 14.3_
  - [~] 8.6 Regenerate an artifact after its Sources change
    - Owner-only route that regenerates in place and records the new Source set
    - Decide what happens to Study_Progress: a card that no longer exists cannot keep a schedule, and quietly discarding a member's history would be the wrong default. Implement the choice and record it
    - _Requirements: 8.5_
  - [~] 8.7 Test schema validation and retry
    - For a response that fails validation, nothing is persisted and the job retries; this is the structured-output risk the design's Risks table names
    - A response that never validates ends as a failed job carrying a reason, not as a silent absence
    - The Sources recorded on an artifact are the Sources fed into it
    - _Requirements: 8.2, 8.3, 8.4_
  - [~] 8.8 Checkpoint — study artifacts
    - Ensure all tests pass, ask the user if questions arise.
    - Re-run 6.5's suite: task 8 added a new category of Notebook-owned data, and every new route is a new place for access to leak

- [ ] 9. Release readiness
  - [~] 9.1 Verify against the v1 goals
    - A member can create a private Notebook, share it with one named person, and revoke that Share
    - A Viewer can study from a shared Notebook without altering it
    - Flashcards, quizzes, and mind maps generate from real course material and survive schema validation
    - Confirm no Source content or query reached any third-party service, and that no Grounded_Answer drew on another Notebook's Sources
    - `tests/integration/test_v1_acceptance.py`, guarded by the same environment check as 3.4, driving the above through the API against the deployed stack
    - Prove the cross-Notebook claim rather than asserting it: seed two Notebooks with disjoint, checkable content and confirm neither answer contains the other's material
    - Prove the no-egress claim from configuration: the app's only registered credential is the gateway, and `deploy/litellm-config.yaml` names only the local backend. Assert no external endpoint appears in the app container's environment, which 2.4 already verified once and which any later credential could undo
    - **Found 2026-09-24, and it changes how the egress proof above must be written.** Every URL Source is fetched through a third party by default. Content settings leave the URL engine at `auto`, and with no `FIRECRAWL_API_KEY` set, content-core's `auto` chain tries Jina first, as `https://r.jina.ai/<url>`, before falling back to a local fetch. The app container has open internet egress — measured: `example.com` 200, `api.openai.com` 401. Jina needs neither a credential nor an environment variable, so a proof "from configuration" would pass while this happens. Either set the URL engine to `simple` (a settings change, no code) or record the exception explicitly
    - _Requirements: 2.4, 3.2, 6.5_
  - [~] 9.2 Confirm outstanding risks
    - Resolve whether OrbStack's commercial licence is covered, or migrate to Colima
    - Re-verify access enforcement after the first upstream merge
    - Merge the next upstream tag onto `eero` and re-run 6.5's suite; Requirement 14.4 is a standing obligation, not a one-off
    - Add a check to `.github/workflows/test.yml` asserting the branch point is a pinned upstream tag rather than upstream's default branch
    - **Manual, not automatable:** the OrbStack licence question is a commercial decision, not a code change
    - **Manual, carried from 4.1:** the `/etc/hosts` entry and trusting the private CA on client devices both need `sudo` this host does not grant, and the devices are out of reach from here
    - **Manual, carried from 4.2:** an off-host copy of the backups, a notification channel on Kuma so a red monitor tells somebody, and `OPEN_NOTEBOOK_ENCRYPTION_KEY` held outside the backups
    - **Found 2026-09-24, and the most serious item on this list: any member can repoint the instance's inference.** `api/routers/credentials.py` resolves no member, and neither the middleware nor `access.py` has any notion of an admin, so being signed in is all it takes. `UpdateCredentialRequest` accepts `base_url`. So a member can point the gateway credential at a server they control and receive every other member's questions *and* the Source text chat sends as context. That is a cross-member disclosure and a breach of Requirement 3.2 at once. Read from the code and **not exercised**, because doing so would break the live deployment. 6.2 and ADR-010 recorded instance configuration as writable by anyone signed in. Neither followed it this far. The API key itself is not disclosed: a member reading credentials gets `has_api_key` as a boolean. The smallest fix is to require the operator (`is_admin`) for writes to credentials, models and settings. That creates the operator role the design does not have, which is why it is a decision and not a patch
    - **Found 2026-09-24:** the Next.js `/api/*` rewrite cuts off any request after 30 s, and 4.1's TLS default routes the browser's API calls through it, since `mon-traefik` sends `eeronotebook.local` to :8502. Once the operator steps are done, synchronous LLM calls — notebook chat on a large context, the transformation playground — will fail past 30 s. Use `API_URL=https://api.eeronotebook.local`, or raise `experimental.proxyTimeout` in `frontend/next.config.ts`. Whichever is chosen, the host's `deploy/.env` override exists only there, so a `.env` rebuilt from the template gets the broken default
    - **Found 2026-09-24, open from 6.3 and now observed:** a Viewer on a shared Source is still offered **Delete Source** in its menu, and **New** insight plus the transformation picker on its Insights tab. The API refuses each with 403, so this is a UI gap, not an access gap. The next Viewer to click one gets an error toast
    - **Found 2026-09-24:** Requirement 2.5 on the chat path rests on the model's disposition, not on an instruction. `prompts/chat/system.jinja` carries citing rules but no refusal rule. Measured today: with the UI's default context, chat answered a covered question correctly and refused an uncovered one. But that is one run. And once a Source has insights, the UI's default quietly switches it to insights-only context, which is the condition under which 3.4 watched chat invent a cited answer
    - **Found 2026-09-24:** a Viewer's reads write to the owner's records. `last_viewed_at` and `updated` live on the notebook and source rows rather than per member, so the owner's "Recently Viewed" and "Updated" reflect other members' activity, and a Viewer sees the owner's history. No content crosses, since the Viewer may read those Sources, but "updated" no longer means the content changed
    - **Before the next audience:** the two demo members `ada@` and `grace@eeronotebook.invalid` share one password, which has been typed in chat. Delete both, or rotate it. `scripts/demo_seed_and_verify.py` is not idempotent: each run adds a fresh pair of Notebooks
    - _Requirements: 14.1, 14.4_

## Notes

- Sub-tasks marked `*` are optional and may be skipped for a faster route to something usable. Top-level tasks are never optional
- 6.5 is a test task and is deliberately **not** optional: Requirement 14.4 re-runs it after every upstream merge, so it is infrastructure rather than assurance
- Sub-task numbers are stable identifiers, so new work appends rather than renumbers. That is why 3.5, 6.4–6.7 and 8.6–8.8 sit after sub-tasks they logically follow
- `[~]` and `[ ]` both mean not started; `[-]` means in progress and `[x]` complete
- Tasks 1–4 prove the deployment against unmodified upstream; 5–9 diverge from it. 3.4 is the gate between them. One application change already landed early — the embed-failure report in 2.4 — because both embedding paths are fire-and-forget and configuration could not reach them
- Start order is database, API, worker, frontend. The worker is not optional: source processing, embeddings and artifact generation are all async jobs that queue silently without it
- Every database query, graph invocation and model call is awaited. Migrations are hard-coded in `AsyncMigrationManager` and run on API startup, so a bad migration stops the app
- Out of scope, recorded so it is not mistaken for an omission: `EarWig/.kiro/docs/MIGRATION_PLAN.md` carries a stale address, `apt` provisioning and `/opt/stacks` paths that do not match this macOS host. It belongs to another repository and is not an eeroNotebook task, but it will mislead the next reader
- Also out of scope per the requirements' Non-Goals: podcasts and TTS, video overviews, infographics, slide export, instructor visibility of member scores, any role beyond owner and Viewer, team-wide libraries, and performance targets

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 0, "tasks": ["2.4"] },
    { "id": 1, "tasks": ["3.4"] },
    { "id": 2, "tasks": ["3.5"] },
    { "id": 3, "tasks": ["5.1"] },
    { "id": 4, "tasks": ["5.2"] },
    { "id": 5, "tasks": ["5.3"] },
    { "id": 6, "tasks": ["5.4", "6.1"] },
    { "id": 7, "tasks": ["5.5", "6.2"] },
    { "id": 8, "tasks": ["6.3"] },
    { "id": 9, "tasks": ["6.4"] },
    { "id": 10, "tasks": ["6.5", "6.6", "7.1"] },
    { "id": 11, "tasks": ["7.2", "8.1"] },
    { "id": 12, "tasks": ["8.2", "8.5"] },
    { "id": 13, "tasks": ["8.3"] },
    { "id": 14, "tasks": ["8.4"] },
    { "id": 15, "tasks": ["8.6"] },
    { "id": 16, "tasks": ["8.7"] },
    { "id": 17, "tasks": ["9.1"] },
    { "id": 18, "tasks": ["9.2"] }
  ]
}
```
